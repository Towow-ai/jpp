# 19 · Nature 决策单（2026-09）

**状态：已授权。** Nature 2026-09-23 授权：技术项由 Fable 裁定后进入依据文本，Nature 保留事后否决权（主会话中直接回答「可以」；原话亦见 `附注/2026-09-23-核心设计讨论-结论.md` 第十三轮；规则见 `00-定位与方法论-v1.md` §5.6；宪法附则二已加附注）。

**Nature 以后只需看本单并点名否决。** 每条第一句是可判是非的话；否决时说「否决 Bn」即可，主会话按该条「所改位置」回退并记账。各条依据、推翻条件、替代与放弃理由见 `附注/2026-09-23-决策单B栏-Fable技术裁定.md` 对应条与 §三；逐处改动明细见 `过程记录/2026-09-23-B栏写入依据文本.md`。B37–B61 是架构方案附录 A 的裁定（架构终审由独立 Fable 通过），六项在 `20-架构方案-v1.md` 附录 A 对应条内自含，见本单 §一·续。

## 一、已授权进入依据文本（2026-09-23）

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B1 | 一道题的结构补五件：主体、谓词、划分、请求、前提 | `12` §2.2 Q 定义与题的五件条 | 已进；已实现（92a4f05）；部分由 B54 修订（前提「不进题哈希」订正为不进 `q_hash`、进 `form_hash`） | Fable 第二轮把四个功能误读为「一道题的四种来源」，第三轮按原话重述，Nature 确认 |
| B2 | 校准记录主键为题式、填法为次键，不按填法或候选类细分 | `12` §2.2 calib_key 条 | 已进；实现为「题键 → 题式键」回退查找（7efa9f4）；第三轮数字为口径未定的临时数 | 主会话「全局线不可行」撤回；第三轮 Sonnet 真值错判数因抽检 78% 降为临时数；第四轮预测方向反了 |
| B3 | 未决原因加 `rejected_all` 与 `no_candidate` | `12` §2.3 cause 枚举；§5 handler 库行 | 已进；已实现 `ea77d7f`（first_k 与空候选 select 产生；库去向 lib/handlers.jpp） | 无 |
| B4 | `insufficient` 是纠正性回答的位置；J-02 不禁题面作另一题的材料 | `12` §2.3；§3 J-02 可判性一格 | 已进 | 无 |
| B5 | derive 定义为：唤出 → 候选池 → 静态诊断 → 信息值打分 → 验题闸门 → 校准上岗 | `12` §5 骨架行 | 已进；未实现（件 k）；B45 补默认路径（确定性派生为默认，`gen` 唤出为后备） | 无 |
| B6 | 四个骨架统一为探问搜索情景树，各给终止论证 | `12` §5 骨架行 | 已进；未实现（件 j） | 无 |
| B7 | 规划器价值函数 = 效用准则 × 可靠性折扣，按状态算密度，切分点由校准信道给出 | `12` §4 pass 6；`05` §5 后注 | 已进；未实现（件 m） | 无 |
| B8 | 宪法登记表 Chow 行加限定：后验为估计值时拒识规则不再最优 | `00-宪法` Chow 行第四格 | 已进 | 无 |
| B9 | 登记表加交互证明重复放大（反面）行；重问降错只在重跑分歧检验通过的键上启用 | `00-宪法` 登记行增补；`12` §5 handler 库行 | 已进；第三轮实测错判重跑 16/16 不变 | 无 |
| B10 | 「带弃权的链式风险-覆盖界」列为深度验收的理论目标，不作本版交付 | `00-目标与动机` §6 第 2 条注 | 已进 | 无 |
| B11 | 索引定义并入 B20，不单独立项 | `12` §5 料库行 | 已进（由 B20 取代） | Fable 两次偏离（材料三层、计算缓存四时机），重读 #31 后并入 B20 |
| B12 | 画像加候选字段：错误相关性、按候选类噪声率、高频单字偏肯定 | `12` §1.2 后注 | 已进，状态为候选，未进档案 schema | 「按候选类噪声率」降为候选，主机制改为弃权带加诊断 |
| B13 | 诊断层规则集第一批（含提到类须声明外延、弃权率是必要非充分信号） | `12` §3 J-17 后；`05` §4 后注 | 已进；静态可判的七条已实现（件 d，工程步 5，提交 97f1dec：`check/diag/b13.rs`，`W-diag-*`）；需判断器的两条（前提被做出、问的东西在材料里）留到步 26 | 曾拟用元题作诊断（P7 否）；弃权率曾拟作充分信号，改为必要信号 |
| B14 | 题式组合加否定或递归前先核分析代价 | `10-组合代数` §11 | 已进 | 无 |
| B15 | 语言规则不写死当前价格与弱点，全部住画像 | （Nature 已确认；`00-定位与方法论`） | 已确认 | 无 |
| B16 | 八项登记为「本项目新增、文献无先例」 | 设计总账标签（非依据文本） | 已进总账待补 | 无 |
| B17 | 组合封闭性契约进语言，三条不变量 | `12` 新增 §2.12；`13` §3 后注 | 已进；施工中（件 i） | Fable 第一轮倾向先做库类型，Nature 确认后改进语言 |
| B18 | Claude 作检验器时意见作材料由判断器读 | `12` §2.5 | 已进；已实现（件 n，c976de8） | 无 |
| B19 | 真值来源：可算全用；语义题模型标注，须人工抽检一致率单侧 95% 下界 ≥ 0.9 | `12` §2.3；§5 校准库行 | 已进；门槛改下界随件 i 实现；复核者依 B36 改为 Fable | 门槛曾按点估计，改按单侧 95% 下界；同源一致率曾被当依据，改为不计 |
| B20 | 材料选择是由判断组成的递归选择，按有用性不按相似度；跨程序变换键不含调用位置 | `12` §5 料库、召回层行；§2.10 注；`13` §4 后注；`05` §4 前注 | 已进；未实现（件 l；键改动未实现）；部分由 B40（运行内复用按缓存键）、B41（增量作废按内容寻址）细化 | 同 B11 |
| B21 | 共享前提题库是受维护的产物，有规范与变更记录 | `12` §5 新增题库行 | 已进；阶段 4 交付；部分由 B49 修订（入库判据去「字面」硬条件）、B48 补治理 | 无 |
| B22 | 读数的合法消费者封闭为八类，账本供审计 | `12` §3 总述 | 已进（Nature 明确确认） | 读数去处曾数为五类，按消费者重数为八类 |
| B23 | 题式的槽声明可接受的填法类型；类别槽声明成员资格口径 | `12` §2.2 | 已进，状态为候选规则，静态报警未实现 | 预注册预测 D 过、R 不过，实际相反；类别档口径由第四轮抽检补入 |
| B24 | 校准阈值两侧联合认证，防多重比较；认证附带材料范围 | `12` §2.3；§5 校准库行 | 已进；两侧拆分认证已实现（ffcd958），范围与漂移监控未实现 | 首版同批选线并认证，改为拆分样本 |
| B25 | 停岗：系统自动标「停岗候选」并停止用该线放行不可逆 do，人只确认候选 | `12` §2.3 CalibRef.status 与判序前条；总账 L-011 改已定未造 | 已进；已实现 `94ae210`（漂移信号自动标候选；`jpp calib-confirm` 人确认） | 无 |
| B26 | Rust 解释器加独立 `.jpp` 文法为已认路线；12 旧「无限期推迟」注为被 13 取代 | `00-宪法` Rust 行；`12` §6 开头注、建造顺序第 8 条注；总账冲突 #1 关闭 | 已进 | 无 |
| B27 | 设计总账五桶加叠加标签「本项目新增、文献无先例」，机读版桶名归并 | `18` 人读版计数表与三条目；机读 JSON 归并（原名存 bucket_original） | 已进 | 术语表首版写四桶，补「被取代」 |
| B28 | 同题重复读数只许取均值或中位数压抖动，过桥用含 n 的独立校准键，出口众数禁用 | `12` §2.2 判断向量后新条；§5 handler 行；总账 G-007（已造出，语义受限）、K-236 | 已进；已实现 `8d8d63d`（`repeat` 取代 `agg`；库里尚无 band → 重跑处理器，启用位 `rerun_independent` 已备）；B40 以本条为例外（`repeat` 读数不经缓存复用） | B 栏 #8 只改名未裁语义，本条补 |
| B29 | J-03 同样约束宿主：记录只由 commission 产出，put 只许夹具记录，夹具线出口标 W-fixture-line 且不放行不可逆 do；前端补 cut 的 cost | `12` §3 J-03 行与 J-03 边界段（原「宿主写线是设计意图」被取代）；§2.9 注；总账 K-143 | 已进；已实现 `e99455d` | 12 曾把 J-03 解释为只拦程序，已纠正 〔B128 / B129 修订（2026-09-25）：作者声明线不受 J-03；`cut` 的 `cost` 表层已造，「已定未造」订正，CLI `--cost` 随 20a-2〕 |
| B30 | 校准键以 `12` §2.2 元组为准，主键 (form_hash, slot_kinds, phys, render_version, literal_mode)，q_text_hash 作次键，代码键为其序列化并迁移一次 | `12` §2.2 校准键结构条；总账 K-222 | 已进；按 B34 施工（类键层已落，步 20b；代码未落：元组序列化、迁移，步 20a）；部分由 B60 修订（元组加 `model`）；**B116（2026-09-25）**：手写题为零槽题式、`q_text_hash` 只作单填法覆盖，映射与迁移见 `附注/2026-09-25-批量裁定.md` §五 (c) | 无 |
| B31 | 不加未决第五去向；等真值是升级，走 ask 的 Pending；标签是校准进料 | `12` §3 J-05 注；§2.6 ask 条；总账 K-044 改被取代 | 已进 | 无 |
| B32 | cause 加 absent 与 latency；budget 加 absent 策略（默认 escalate、熔断）与 latency_p95；超时转 Unsure(latency)；规划器拒超时计划 | `12` §2.3 cause 枚举与判序前条；§2.7 budget 注；总账 K-039、K-041 | 已进；已实现 `1c66ba9`（未声明 absent 时沿用旧行为，见过程记录） | 无 |
| B34 | 作者写的校准键降为校准类别标签，记录以元组为主键；查找顺序 题键 → 题式键 → 类键 → 模式键 → 冷；W-class-line、W-class-ambiguous | `12` §2.2 校准类别标签条 | 查找链已实现（步 20b，见过程记录 `工程-步20b.md`）：题键 → 题式键 → 类键 → 冷，类记录存于 `\u{1f}class\u{1f}<标签>`，`W-class-line`，B68 范围按类记录核；`calib-import` 的 `class` 行导类记录。待 B75：类线暂不放行不可逆 `do`、「混合样本」临时口径为来源题 ≥ 2。未实现：元组主键与 `class` 字段（步 20a）、`W-class-ambiguous` 静态面（步 24）；部分由 B44 修订（模式键移出查找链）；**B116（2026-09-25）**：作者串 20a-2 后不再命名记录，查找链加夹具键、`put` 记录进夹具命名空间，迁移带 `--questions` | 无 |
| B35 | 重放缺记录报 E-replay（致命，不进 cause）；判断器缺席记 cause absent，语言默认 escalate | `12` §2.3 cause 注与 B32 条 | 未实现 | 无 |
| B33 | 宿主标量值带一位 taint，宿主内按值传播显式数据流，控制流与判断器内部不传播，替换旁路表机制 | `12` §2.11 第 4 点改写与「宿主内传播」段；`00-宪法` IFC 行；总账 K-119、K-124、K-130、K-182、K-203 | 已实现 d29e48d；部分由 B58 修订（`judge` 输出取状态 taint ∨ 题 taint） | 主会话把残余描述为「单个数字、单个字」，实测可达残余只有一位数字的 JSON 数值一条 |
| B36 | 真值复核改由 Fable：语义题式由 Sonnet 标注、Fable 复核批次作基准（一致率单侧 95% 下界 ≥ 0.9，30 条批次），不再人工抽检；可算真值优先；弃权率高或分歧集中同向的题式判外延未定、先改题面；复核者须与标注者、材料生成者不同；复核一致率不证明与人一致，出现人工或外部真值系统性不一致即退回 | `12` §2.3 B19 条与 B24 补充半句；§5 校准库行；本单 §四 | 依据已改；代码已实现复核行识别、同源拒收、取序、gate 文本（工程步 2，2c6fdbf）；「分歧集中同向或同一填法档判外延未定」已实现（工程 B36-5c，提交见过程记录 `工程-B36-5c.md`；阈值为导入参数） | 任务书把授权写成字面替换「人工→Fable」；本条更正：复核一致率证明两模型口径一致，不证明与人一致，故加限定与推翻条件 |
| 术语 | 依据文本中分类义的「题式」改称「题类」；K-107「只能」改「首要」；K-185 与 B 栏无矛盾，已确认 | `12` §10 对照表三处；总账 K-017（改「题型」）、K-158、K-107、K-185 | 已进 | 无 |
| 宪法破口栏 | 推测执行行与 IFC 行各加「实测破口与修复提交号」，提交号待回填 | `00-宪法` 两行下各加一行子行 | 已进；提交号「修复进行中」 | 无 |
| 登记行 | 问题理论调研登记行增补 26 行（综述 28 行中「拒绝选项」并入 Chow 行、「信息价值」两行合并） | `00-宪法` 末节「登记行增补（2026-09-23）」 | 已进 | 无 |
| 施工顺序 | 按依赖排：a、b 为根，c、d 随 b，e/f/g/h，i，j，k，l/m | `17-总规划-v1` 阶段 3 施工顺序表 | 已并入 | 无 |

## 一·续、架构终审裁定进入依据文本（B37–B61，2026-09-23 晚）

来源 `20-架构方案-v1.md` 附录 A；独立 Fable 终审有条件通过，条件已改入 `20`、`21`（`20` 文末终审记录）。依 §5.6 授权写入，Nature 可点名否决。六项（为什么、替代与放弃、依赖、推翻条件、怎么改、理解偏差与更正）在附录 A 对应条内自含；依据文本各处写入的是结论加理由、替代、推翻条件，每处带「依 Nature 2026-09-23 授权，架构终审通过，裁定 Bn」。代码步号指 `21-工程方案-v1.md` §四·2。编号是终审改号后的编号（初稿与三份评审里的旧号加一即新号）。

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B37 | 五种效应各有画像分表；只有 `judge` 有类假设与降级规则；`gen` 的 `taint_out` 默认 `inherit` | `12` §1.2 后「其他效应的画像项」；§2.4 `gen` 输出 taint；§2.11 表 `gen` 行 | 依据已改；代码按 `21` 步 15d 实现 | 无 |
| B38 | 多效应共用一个预算池，按实例画像计价；`budget.calls` 计所有效应调用；`latency_p95` 按层取最慢效应；多读数实例路由不裁 | `12` §2.7 末 | 依据已改；代码按 `21` 步 15d；`latency_p95` 按层在步 23 实现 | 修订前以「两个会话」回避多判断器，与定位 §二冲突；改为数据模型留位、路由待裁 |
| B39 | 降级表按「假设为假时的失败方式」逐行定方向；H7 融合开但估计按假设为假计；`one_hop` 补行，未测按 `true` | `12` §1.3 表八行与 `one_hop` 行、表后 B39 条；§3 J-15 行；§2.11 J-15 旧述加注 | 依据已改；代码按 `21` 步 23 实现 | `12` 原文「取该假设为真」一刀切曾被订正为逐条，本条写全逐条答案；数值字段未测行为评审后补表（`20` §3.9） 〔B126 修订：H1 / H8 降级落点——守卫半 `E-modality` 步 24，渲染半 S 库读画像 步 25〕 |
| B40 | 读数运行内复用按缓存键；账本键保留 `site` 与 `run_seq`；复用条目标 `reused_from`、`cost: 0`，计费与观察剔除；`repeat` 读数例外 | `12` §2.10 表后 B40 条 | 依据已改；代码按 `21` 步 19 实现 | 无 |
| B41 | 增量作废按内容寻址，不做依赖图；存量按最近命中运行数在料库预算内淘汰，账本不删 | `12` §5 料库行 | 依据已改；代码按 `21` 步 19、步 29 实现 | 无 |
| B42 | 计划期估计可为「未知」；只有可靠下界（账本空且缓存关）超预算才拒，其余告警；层边界核是运行期强制点 | `12` §4 pass 6（原「只告警不拒」）；宪法 E12 行不改 | 依据已改；代码按 `21` 步 22 实现 | 无 |
| B43 | 「何时再问一层」由程序控制流决定，规划器只在超额时按价值密度挑本层子集、不能多问；三条终止线是语义；深度缝一半在库如实登记 | `12` §2.7 深度缝两半条；§4 pass 6 层内挑选 | 依据已改；代码按 `21` 步 22 实现 | 修订前 §2.5 只登记终止线并标「不可绕过」，隐藏了与 `00-目标与动机` §三的差距；现如实登记 |
| B44 | 未认证题式默认只能 `Unsure(cold)`；借线只经类键；模式级记录移出查找链，只作 `commission` 先验 | `12` §2.2 B34 查找顺序、B2 实现现状注、新 B44 条 | 依据已改；已实现：模式键出链（步 11b-2，`d8dbc577`），借线只经类键（步 20b） | Nature「生成的题必须有线」的落点是 derive 的校准上岗，不是给未认证题一条线；修订前查找链末端仍有模式键 |
| B45 | 出题以确定性派生为默认、`gen` 唤出为后备，同过验题闸门；选出的题面须在留出集上不劣于原版；唤出守则随 `lib_version` 版本化 | `12` §5 骨架行 | 依据已改；代码按 `21` 步 28 实现 | 无 |
| B46 | 题类作为只读字段 `Question.kind` 进 IR，由 `op × slot_shape` 推出，作者不可写；题的模块清单十项 | `12` §2.2 题类条；§5 题类行（原「不进 IR」） | 依据已改；代码按 `21` 步 12d 实现 | 无 |
| B47 | 诊断层是检查器里的一个闸门函数；运行时经标准库注册的宿主变换调用，运行时不依赖检查器；校准上岗在两次运行之间 | `12` §3 J-17 后诊断层注 | 依据已改；代码按 `21` 步 5（函数）、步 26（运行期注册） 实现 | 无 |
| B48 | 题库目录级、带版本、条目以 `form_hash` 为身份；入库须装载时重跑认证通过；版本或判断器变化后全库重认 | `12` §5 题库行；`题库/规范.md` 的对应四处待原型分支 `488218a` 合并后改（main 上无该文件） | 依据已改；代码按 `21` 步 27 实现 | 无 |
| B49 | 题库入库判据去掉「字面」硬条件，改为「多个目的的共享前提、已认证、便宜」；「字面」降为诊断信号 | `12` §5 题库行（原「字面」） | 依据已改；代码按 `21` 步 27 实现 | 无 |
| B50 | 共享前提题本版由人从题库指定；规划器只在同一题式被 ≥ 2 个目的引用时提示预处理划算，不自动预处理 | `12` §5 料库行 | 依据已改；代码按 `21` 步 29 实现 | 无 |
| B51 | `15` 未入账的九条逐裁：R6、R10 采纳（经 B45 进）；R1、R2 采纳、R3 采纳一半、C1 采纳；R4 不采纳；R7 本版不采纳（归 B10）；C3 不裁 | R6、R10 随 B45 进 `12` §5 骨架行；R3 采纳的一半（「都不是」由作者显式给）已由 `12` §1.2 H2 与 B39 覆盖，无需另写；R1、R2、C1 曾因附录 A 缺逐项推翻条件与替代而按 §5.6 退回，2026-09-23 晚 Fable 按六项补为 `20` 附录 A B51-R1、B51-R2、B51-C1 后写入：R1 进 `12` §5 聚合子行（保守合成三聚合子），R2 进 `12` §2.5 `Action` 行与 §5 S 库行（候选字段 `mat_shape`），C1 改写 `12` §4 pass 8 行；R4、R7、C3 只进总账（L-070、K-242、G-058） | 依据已改；代码按 `21` 步 25（R1）、15d 与 24（R2）、13b 与 22（C1）实现 | R1：`15`「任一分量 unsure 即 unsure」在最坏元素已出现时不成立，改为最坏元素支配 unsure；首稿只写档一种，补全三种。R2：首稿字段名 `output_shape` 与 `20` §2.3 `EffectSpec.output_shape` 撞名，改 `mat_shape`；落点从只在 `Action` 扩到带 `taint_out` 的声明行。C1：「零边际」是画像 H7 的内容不是规则常量；落点是整行改写不是一句 〔B147 修订：R2 的 H4/H5 触发方向订正为 B39 不利侧〕 |
| B52 | 未决责任按可达性核归属，延伸进闭包捕获；闭包只在是唯一可达路径时为 `Fn¹`；不建完整线性类型 | `13` §3 末 B52 注 | 依据已改；代码按 `21` 步 21 实现 | 修订前把「别名不构成两份证据」误推成「别名即错」 |
| B53 | 内核六形式消融各对应一条会红的测试，没有测试红的形式并入其他形式 | `12` §8 第 12 项 | 依据已改；代码按 `21` 步 31 实现 | 无 |
| B54 | 前提声明进 `form_hash`（校准键），不进 `q_hash`（账本键与缓存键） | `12` §2.2 题的五件条（原「不进题哈希」） | 依据已改；代码按 `21` 步 8（哈希分工）、步 27 实现 | 无 |
| B55 | 不可逆 `do` 发出前写意向条目、返回后写效应条目，逐行落盘；续接时有意向无效应的不可逆动作不重执行，出 `Fail(UnknownOutcome)` | `13` §5 末 B55 注 | 依据已改；代码按 `21` 步 7（条目变体）、步 18（落盘） 实现 | 无 |
| B56 | 效应形式名与内核构造名不作一等值，出现在非调用位置报 `E-form-as-value` | `12` §2 开头 B56 注 | 依据已改；代码按 `21` 步 12d 实现 | 无 |
| B57 | 内核构造只收必须用 `.jpp` 拿不到的内核能力的构造；四骨架与 `tally`、`first_k`、`iterate` 为标准库 `.jpp` 函数 | `12` §5 表后 B57 注（不改表） | 依据已改；代码按 `21` 步 25 实现 | 无 |
| B58 | 题面带 taint（槽填入值 taint 之 ∨）；读数效应输出 taint = 状态 taint ∨ 题 taint | `12` §2.11 表加行、`cut` 行、B33 第 4 条；§2.3 出口 taint 句 | 依据已改；代码按 `21` 步 17 实现 | 无 |
| B59 | 「跳」按依赖计，材料依赖与题的派生依赖都算；账本判断条目记 `parents` 与 `hop`，验收 2 的深度曲线从账本取数 | 无依据文本位置（数据模型，只在 `20` §3 与附录 A）；`20b` Q15 仍标「否」，须按本条改标「影响数据模型：是（深度半）」（未改） | 已裁，依据文本无改动；代码按 `21` 步 17 实现 | 无 |
| B60 | 效应实例 = 效应种类 × 模型 id；账本键、缓存键、校准键各含 `model`；`via` 缺省为会话默认实例；多实例路由不裁 | `12` §2.2 B30 条补注与新 B60 条 | 依据已改；代码按 `21` 键在步 7；画像与端口在步 15b、15d 实现 | 无 〔B127 修订：`model` 分量随 20a-2、15g (D2) 作门禁；过渡期 `--calib` 只对 `fixed` 与 `live` 开放〕 |
| B61 | 账本头 `budget` 只记录、不进比对；续接时预算变大不报 `W-header` | `12` §2.10 账本头行与 B61 条；§3 J-18 行 | 依据已改；代码按 `21` 步 7；步 18 断言 实现 | 修订前「budget 不比对」只写在评审处理记录里，未作为依据文本改动裁定；终审补为本条 |

## 一·续二、探针首轮裁定进入依据文本（B62–B70，2026-09-24）

来源 `附注/2026-09-24-探针首轮裁定.md`（阶段 5 首轮探针 `过程记录/阶段5-探针-首轮.md` 的设计缺口 D-1…D-5 与实现缺口 I-1…I-9 的裁定；写成新附注而不追加 `20` 附录 A 的理由见该文件开头）。依 §5.6 授权写入，Nature 可点名否决。六项在该附注各条内自含；依据文本各处写入的是结论加理由、替代、推翻条件，每处带「依 Nature 2026-09-23 授权，Fable 裁定 Bn（地基/附注/2026-09-24-探针首轮裁定.md）」。代码步号指 `21` §四·5。

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B62 | D-1 不成立：n 个主体共享语境 = n 个状态共享 `ctx`、同层并发；不引入多主体状态，不把多对象放进一个 `on` 由题面按编号引用（J-14/H8、B1/B46、B24）；它不是 `16` §一十四倍问题的另一形态；补 J-14 运行期面 `W-multi-object`（I-10） | `12` §2.1 `on` 行；§3 J-14 行 | 依据已改；I-10 按 `21` 步 24 | 探针把「一次调用」当目标；按 P5 目标是「一层」，W-2「成本转到作者」撤回 |
| B63 | D-2 部分成立：K 元划分（`select`、`measure`）的线是胜出候选概率上的单侧线 `hi` 带 δ 迟滞，认证与 B24 上侧同形，真值通道收候选/档位索引；「档位即动作」是划分语义不是免线理由 | `12` §2.3 B24 条后新条 | 依据已改；代码已实现（步 20e，见 `过程记录/工程-步20e.md`）；select 真机仍需步 15f 的置换声明；真机 V5 见过程记录 | 探针把 D-2 写成「认证一条线 / 全部未决」二选一；缺的是 K 元划分的认证形式与进料 |
| B64 | 置换是 `select` 站点的测量声明 `permute: true`（题式或题上），默认不开，未声明出口 `Unsure(untested:permutation)`；不加 CLI 开关；画像 `position_bias` 免置换规则留位待裁 | `12` §2.3 `Pick` 行 | 依据已改；代码按 `21` 步 15f | 无 |
| B65 | D-3 不成立：缓存写入由料库承担（B20/B41/T6）；可逆且幂等的内容寻址写入按 `reversible = true` 登记不受 J-08；补宿主取回入口与 S 库 `store_put` | `12` §5 料库行、S 库行 | 依据已改；代码按 `21` 步 18、29 | 探针把 J-08 拦下读成缺存储效应；缺的是动作的可逆性声明与取回入口 |
| B66 | D-4 不成立：宽度控制已有 `select` 于兄弟节点、`budget` 加层内挑选（B43）、锦标赛骨架三条路径；按概率排序截断维持禁止 | 无依据文本改动 | 已裁；`21` 步 22、25、30 已有 | 探针 F-2「失去宽度控制手段」是读法偏差 |
| B67 | D-5 不成立：只作证据的题不加「不参与路由」标注，未决随记录交出（B17）；`W-drop-vs-escalate` 不改 | 无依据文本改动 | 已裁 | 无 |
| B68 | I-9 是本轮唯一放行方向缺口：认证范围最小形式（可算材料指纹、分位区间为导入参数）；范围外出口与夹具线同级，报 `W-calib-scope`、不放行不可逆 `do`；步 20d 拆出范围一半提前。**修订（2026-09-24）**：区间 = 分位区间加边距——尺度量（字符数、行数、平均行长）×/÷ k = 2，比例量 ± m = 0.10；k、m 为导入参数；认证集自判范围外须为 0 | `12` §2.3 B24 条后新条；`20` §3.4 等级表加 `OutOfScope`（附注标出，下一版合入）；修订：`12` 同条改写、附注 B68 修订段 | 依据已改；原口径已实现（步 20d-1，`a6c34447`）；探针复跑触发推翻条件 (1)（同风格对照 83% 判范围外，全因字符数区间过窄）；**修订已裁并已实现（`ceb6daca`）**：`from_texts` 加边距、`calib-import --scope-margins`、`W-scope-self`；重导入 `probes/scope/calib` 后 `crates/jpp-calib/tests/scope_probe_reconcile.rs` 与 Rust 之外预期 13 组逐项相等（认证集 0/410、同风格 0/33、跨风格 178/178、拼接 3 句起判出）；程序级 topic-relevance 0/36（修订前 10/12）；**B104（2026-09-25）**：无指纹 = 范围未知、路由不变、不放行（`W-scope-unknown`），旧记录同此；部分带文本按带文本样本写指纹并记 `n_text / n`；随步 20h-1 | 原裁定把认证集的散布当风格的散布；生成集的长度窄带是 prompt 产物。修订把散布（分位）与容差（边距）分开；实施者的中位数倍数候选在宽集上不自洽，改为对分位界乘除 |
| B69 | J-06 静态面接受顶层单赋值整数字面量常量作 `bound`（A-3a）；A-3b 已裁 B111（2026-09-25，调用点一层常量传播） | `12` §3 J-06 行 | 依据已改；代码按 `21` 步 24 | 无 |
| B70 | 宿主 Int/Float 混用统一提升为 Float；整数间语义按 `13` §6 不变 | `12` §2.7 | 依据已改；代码按 `21` 步 14c | 无 |
| 方法论 | 预注册必须在花钱前写进过程记录并提交（有提交号），事后写的预测不计为检验 | `00-定位与方法论` §5.2 加一句（非依据文本，Fable 直接改） | 已改 | 探针首轮的预测未事前落盘，报告 §二已标注 |

## 一·续三、施工中裁定进入依据文本（B71–，2026-09-24）

来源 `附注/2026-09-24-施工中裁定.md`（施工步提出、须在后续步前裁定的架构边界问题；写成新附注而不追加 `20` 附录 A 的理由见该文件开头）。依 §5.6 授权写入，Nature 可点名否决。六项在该附注各条内自含；`20` 的对应段落以附注标出修订、下一版合入（`21` §五·3 第 3 步）。

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B71 | 规则的读面是 `20` 已定的 `Cx`（IR 含站点表、`check` 的输入、`requires` 声明的分析结果只读视图），只读由 `&Cx` 借用保证；`AnnotTable` 不加字段，只装 L3 之外有读者的标注；IR 不加推断类别；站点表已表达的循环 / 函数归属由规则直接读 IR，`spans.rs` 与 `Ctx` 撤 | `12`、`13` 无改动；`20` §2.3 三处措辞（附注 §二标出，下一版合入） | 已裁；代码按 12b-5（R，12d 前）与 12d | 12b 把「只读标注与 IR」读成标注表是唯一分析入口、把档案 H5 位当内部状态、把作者标注的类型化与表达式类别推断混为一谈；更正见附注 |

## 一·续四、评估①裁定与 Nature 决定进入依据文本（B72–B77、A-6、仪表，2026-09-24）

来源 `附注/2026-09-24-评估①裁定.md`（阶段评估①候选裁定 B72–B74 的独立复核，主会话追加的 B75–B77，与验收 1 的 A-6）。B 号依 §5.6 授权由 Fable 裁定写入，Nature 可点名否决；A-6 与仪表两行是 Nature 2026-09-24 的决定，技术口径部分由 Fable 定。六项在该附注各条内自含。写入已完成（B0128：`12` 21130537、`20` v2 ffc7ac9f、`21` a63d80c5、`17` 848780a8、`20b` acc1ce47、`18` c75dc5ab）；写入时登记的冲突与歧义由 Fable 在该附注 §十裁定并落进各文件（2026-09-24；`21` §三·1 续重排、步 7b/0b、`20` v2 §3.4 等级与条件两表等）。

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B72 | 认证等级按 α 分档：`alpha_trial`（缺省 0.25，n_needed = 9）下的线记 `Trial` 等级，可路由、报 `W-trial-line`、不放行不可逆 `do`；正式 α 与门槛不变；导入先正式后试用；步 17 加谱系放行（材料经 `from_key` 可达的祖先出口须全为放行等级）。修改后采纳（评估者按样本量分档与「30–40 条」不采） | `12` §2.3 B24 条后新条；`20` §3.4 等级表加 `Trial`（附注标出）；`21` 新步 20f、步 17 追加；帮助文本两档样本量 | 已裁，未实现；离线对照 `评估/2026-09-24-B72对照/`（预注册 e97f19e7 先于运行）：试用线上界守住率 ≥ 92%，实际下限字面 60–80 条、语义 80–100 条，正式线字面约 160 条、语义 200 条不保证 | 评估者与裁定者都把「每侧每半 ≥ 8」读成约 32 条，漏了分半随机与两侧平衡；评估者的推翻条件比的是不同使用区间的两条线，改为有效性判据加「先正式后试用」 |
| B73 | `--backend live` 必须有画像：CLI 解析 `--profile` 或 `--profiles-dir/<model>.json`，解析不到报 `E-profile-missing`；画像哈希进账本头；价格从画像读、删 `4.2e-8`；发行附 `profiles/jev-1.13.0.json`；内核删 δ 与线的兜底留步 15d。修改后采纳 | `12` §1.2 画像条一句；`INTERFACE.md:218–223` 与 `options.rs` 帮助文本；`21` 新步 15d-0 | 已裁，未实现 | `INTERFACE`「读不到档案就报错」只覆盖给了路径的情形，CLI 无路径走 `Profile::default()` 且裸真机不打印提示；两口径合一 |
| B74 | crate 上限 11（现有 9 加 `jpp-plan`、`jpp-lib`）；`jpp-store`、`jpp-backend-jev` 作 `jpp` 模块（`store/`、`backends/jev/` feature `live`）；`jpp` 与 `jpp-cli` 合并为 lib + bin 一个 crate；步 23 拆 23a（`schedule` + J-15 降级方向，照建）与 23b（`fission`、`lower`、`plan` 三 pass 以触发 `20` §3.9 行的示例为建造条件）。修改后采纳（冻结在 9 与 `Session` 进 `jpp-runtime` 不采） | `20` v2：§2.1、§2.2、§2.3、§五 S1/S8、§6.1、§6.2、§8.1、§11.1、终审记录第 9 条加注；`21` 步 14a、15b、18、23、32、§七·1；`deps.py` | 已裁，未实现 | 评估者「四个 pass 无消费者」来自 `INTERFACE` 旧注，`20` §3.9 与 B38/B42/B43 已给消费者；「9 个」把 `jpp-plan` 冻掉，与 §2.2 第 3 条（D1.3 的 Cargo 保证）冲突 |
| B75 | (a) `Class` 等级不放行不可逆 `do`，`20` §3.4 为准、`12` B34 条第 (2) 句改；放行判定一处（20a 前 `Exit` 位、20a 后 `LineGrade::releases()`）；类线放行升格路径为留一来源认证通过后 `class_release: true`（本版不实现）。(b) 混合样本：标注行加 `class`，来源 = `form_hash`（无题式取 `q_text_hash`），同题式不同填法同一来源，来源 ≥ 2、每来源 ≥ 该档 n_needed，按来源分层分半，记录写 `sources`，不满足停待真值。采纳 | `12` §2.2 B34 条第 (2) 句；§2.3 B19 条后一句；`20` §3.4 `Class` 行加注；`21` 步 20b 预注册差异 | 已裁；20b 施工中按此实现 | 校准轨曾把不同填法当不同来源以便用 R 数据造类记录；更正为同一来源，S4D + S4R 是现成的两来源数据 |
| B76 | 题类映射表 `question_kind(op, request, 状态槽形, 题式槽声明)`，函数在 `jpp-ir`；不唯一处由题式槽表声明（`over_kind`、`accepts`），缺声明按缺省并记 `kind_source`，矛盾报 `E-kind-conflict`；`kind` 不进 `q_hash`、`form_hash`、账本；基础类在 `jpp-value::Question`（派生字段），精化类在 `ReadingMeta`；不加 IR 节点、不加 `AnnotTable` 字段；落新步 12e（12e-1 检查器轨、12e-2 运行时轨）。采纳（补 B46 缺项） | `12` §2.2 题类条与 §5 题类行；`20` §2.3、§3.3「放在 `jpp-syntax::lower` 旁」改 `jpp-ir::question_kind`，B46 的「总账 N4、N7」改 L-067、K-158；`21` 新步 12e | 已裁，未实现 | B46「进 IR」指 `20` §3.3 的题数据模型，不是 IR 节点；N4、N7 在 `18` 不存在 |
| B77 | 账本头加 `calib_used_hash`（本趟命中记录集合的哈希）；`calib_hash`（装载库）保留只记录；J-18 比对：重放比 `calib_used_hash`，续接两者都比；与 B73 同步落地，金样只作废一次。采纳（评估者修法 (2) 的子集形式；修法 (1) 因续接失去装载库信息不采） | `12` §2.10 账本头行；`jpp-ledger::HeaderCompared`（十一字段，`serde(default)`）；`interp/outcome.rs`、`run_io.rs`；`21` 新步（运行时轨，紧邻 15d-0） | 已裁，未实现；只凭账本重放 34 组中 11 组的假 `W-header` 由此消除 | 无 〔B124 修订：18a 起 `calib_used_hash` 移出 `HeaderCompared`，改 `CalibUsed` 条目逐键比；分支语义不变〕 |
| A-6（**Nature 决定**） | 验收 1 通过线锚定在有出处的真实倍率上，不用「20 倍」；参考系取「专用语言对通用语言」档：只算 J++ 源码 9–20 倍（GAL、BOOM-MR、Sawzall、Jones C→SQL），计入胶水 3.5–4.6 倍，两口径并列，判定写低于 / 达到 / 高于；「十几二十倍」保留为目标量级。技术口径（Fable）：Prechelt 式受控多实现对比，非注释非空行数与完成时间并列，COCOMO II 生成器计数规则，数据行不计、协调代码作子类、调用者方法按触碰内容归段、基线由不参与设计的代理按规程写、基线只实现任务书要求的东西；能力对照 12 项 | `21` 步 31-0 与 31、§四·4 A-6 行；`17` 阶段评估条；`20b` Q15；`probes/*/measure.toml`；`scripts/measure_expr.py` | Nature 决定 2026-09-24；口径已裁；待 Nature 选：主判定用口径一（本文取法）还是口径二 | 主会话第一版转述「≥ 10 倍」被 Nature 随后的原话取代；评估文档五处错误以 `研究/08` 为准 |
| 仪表（**Nature 决定**） | 评测是可反复运行的仪表：每个里程碑与每个 S 步后跑 `scripts/dashboard.py`，结果写黑板 `评估/仪表`；七项：表达量比（两口径、协调代码占比、时间比）、真机新题已决出口率、深度曲线、换画像通过数、等价写法调用比、能力对照通过项、重放一致；最小版本为新步 31-0，紧接 12d，允许某项暂「无数」但管道搭起、基线按参考系记录；步 31 改为补全 | `21` 新步 31-0、步 31 改写；`17` 阶段评估条与里程碑 ② 门槛（六项至少五项有数） | Nature 决定 2026-09-24；未实现 | 无 |

## 一·续五、仪表读数 1 裁定进入依据文本（B78–B82，2026-09-24）

来源 `附注/2026-09-24-仪表读数1裁定.md`（对 `评估/2026-09-24-仪表读数1诊断.md` 候选裁定 B78–B82 与规划建议 6–8 的独立复核，裁定者未参与探针、基线、仪表与诊断）。依 §5.6 授权写入，Nature 可点名否决。六项在该附注各条内自含。写入：`12` §2.12、§3 J-05 行；`20` v2 §2.5、§3.6；`21` §三·1 续、步 25、步 31、§四·4 A-2、新节 §四·7（步 14b-0、25-0、31-0b、31-1、31-2）；`17` 验收 1 注、阶段评估条、里程碑 ②③；`20b` Q15、T2、S3。只有 Nature 能答的两问见该附注 §七（「十几二十倍」对 T1 还是 T0；口径主判定）。

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B78 | 表达量比计入规则：基线侧在口径一与口径二都全计（含阈值换算、三态分支、未决收集），运行时职责行只作占比报告与能力对照 B2 取数，`caliber1_literal` 删；夹具脚本是两侧共用的测试设施（基线经不计行的 SDK 读 J++ 侧同一份夹具，`measure_expr.py:264`），不计入口径二；口径二只计生产必需胶水（CLI 或 `Session` 调用、动作登记、`calib-import` 调用）。采纳；诊断所述事实逐条核对属实 | `measure_expr.py`、`dashboard.py`、`评估/仪表/*.md`、`probes/*/measure.toml`（步 31-0b）；`附注/评估①裁定` §八·3 加注 | 已裁，未实现（步 31-0b，S 小，测试轨） | A-6 §八·3「口径二另计入……夹具脚本……基线侧运行时职责代码」两处是措辞错误，本意由基线规程「只实现任务书要求的东西」承载 |
| B79 | 任务书分 T0（最小，现状）与 T1（完整职责）两档：T1 把账本与重放、预算停机、合批与同层轮数、由带真值标注经两侧二项上界 ≤ α 认证的线四项写成可验收功能需求（附注 §三·3），T0 对 LMQL 带 2.7–4.3、T1 对 9–20 带；两档基线都由非设计者写，J++ 侧另请非设计者写一份取中位；J++ 侧 T1 夹具不带校准字面量、线只从 `labels.jsonl` 经 `calib-import` 来。修改后采纳（并发按 SDK 轮数不按墙钟；「达到该档按 T1 判」改为按 Nature 对目标量级的答复判，答复前按 T1 写并标待确认） | `probes/_baseline/任务书-通用.md` T1 节、五份 `任务书.md`、`jev_interface.py`（`judge_batch`、`rounds`）、`certify_ref.py`、`measure.toml tier`、`measure_expr.py --tier`（步 31-1）；`17` 里程碑 ②③；`21` 步 31 | 已裁，未实现（步 31-1）；预注册 T1 口径一中位约 4.9（4–6） | 诊断把「达到该档按 T1 判」写成技术裁定，其中「目标量级对谁说」是 Nature 的意图，改为两种取法并列交 Nature（附注 §三·4） |
| B80 | 表达量比单位：主读数为折行归一行数比（100 列、CJK 双宽），位置判定用它；原始行数比、token 比、调用数比（固定观察下基线调用 / J++ 调用，逐项目不取中位）并列；原始行数比与 token 比相差 > 1.5× 标「排版差异」。修改后采纳（不采「主读数写成 [token 比, 行数比] 区间、以下沿判」：参考带单位是行，token 不在带的单位里） | `measure_expr.py`（`wrap100`、`calls_ratio`、`layout_flag`）、`dashboard.py`、`评估/仪表/*.md`（步 31-0b）；`17` 里程碑门槛 | 已裁，未实现（步 31-0b）；推翻：token 化对某侧偏置 > 15% 改 AST 节点计数 | 诊断把 token 比当可判位置的下沿，混了单位 |
| B81 | 契约值的编号、投影与消费：(a) `sieve`/`pair` 输出元素保留输入元素的全部字段并加或更新 `item`、`pos`、`trail`、`exit`、`cause`；`index` 只在输入不是元素记录时赋为输入位置、此后经任何构造不变；`pos` 为本层位置；`pair` 的 `left`/`right`/`at` 穿过 `sieve`；`source` 撤。(b) `lib/outcome.jpp` 读法函数作 prelude（随步 25）。(c) 未决条目 = 带 `exit` 的元素记录本身；条目整体进入返回值即转移，投影掉 `exit` 的记录不是转移（`13` §3「只读原因文本不构成处理」的具体形式）；`pending:` 不必写的前提是 `undecided(o)` 与 `unobserved(o)`（或 `o.pending`）都被返回或交出；静态面不加「投影即消费」规则。A-2（`spent` 由运行时从 `evidence` 算，`outcome()` 不收）一并采纳。修改后采纳 | `12` §2.12 B17 条、§3 J-05 行；`20` v2 §3.6；`21` 步 25-0（`sieve.rs`、`pair.rs`、`tally.rs`、`first_k`、`jpp-value/contract.rs::pending_entry`、`constructs/contract.rs`、`INTERFACE.md`、`GUIDE.md`、金样返回值预注册、`tests/bypass/j05_projection_drop.rs`、`j05_entry_return.rs`）；步 24 可选 `W-unobserved-unreturned` | 已裁，未实现（步 25-0；(b) 随步 25）；预注册 refund/interview/winnow/align/folio 计入行 −4/−3/−2/−1/−2 | 诊断 (c)「投影进返回值即计为消费」是放行方向：调用者拿不到出口与责任句柄，且挡不住 `unobserved` 漏项；改为形状改动加运行期可达性核不变。`pending:` 必写是运行期可达性核的要求，不是静态面的 |
| B82 | 多题与多填法的 `sieve` 返回一个契约值：元素集 = 材料 × 题，每个元素另带 `q`、`qi`（填法形式另带 `fill`，为调用者的整个填法记录，`sieve` 只把槽键交给 `fill()`）；`value`、`detail.ignore`、`pending` 与单题同型，读法函数含义不随题数变；`pending` 一份、`evidence` 并集、`spent` 一份（删 `sieve.rs:147–156` 「只记第一份」特判）；单题是 K = 1 特例，无 `[0]`；prelude 加 `by_q(o, k)`（别名 `by_fill`）返回子契约值；`cut` 收读数列表。修改后采纳（不采「`value` 为按填法索引的 `{fill, accepted, ignored}` 记录」：读法函数返回类型随题数变，封闭未落到产物） | `12` §2.12 B17 条；`20` v2 §2.5、§3.6；`21` 步 25-0（`sieve.rs`、`bridge.rs`/`host_builtins.rs`、`lib/outcome.jpp`、`INTERFACE.md` §三·四·一、金样与探针返回值预注册；账本、出口、调用数、层数不变） | 已裁，未实现（步 25-0，与 B81 同步，先于 25、27a、31-2）；预注册 folio 50–57 七行 → 五行、约 264 → 150 token，winnow-batched 15–22 八行 → 四行 | 诊断把封闭性落在「按填法索引的记录」，更正为落在元素集；诊断的 13b 层数推翻条件与本条无关（登记路径不动），改为预注册项 |

规划建议 6–8（`--input` 提前为步 14b-0；步 31 拆 31-0b、31-1、31-2；`17` 门槛按两档与 B80 单位）依 §5.6 由 Fable 与主会话决定，已落 `21` §三·1 续与 §四·7、`17`。

## 一·续六、续接缺口与值级来源裁定进入依据文本（B83–B84，2026-09-24）

来源 `附注/2026-09-24-B83续接缺口裁定.md`（步 7b 施工记录报出的续接缺口，主会话追加的值级来源；裁定者未参与 7b、17a 施工）。依 §5.6 授权写入，Nature 可点名否决。六项在该附注各条内自含。写入：`12` §2.10 B83 条、§3 J-18 行、§2.11 宿主内传播第 1 条注与第 9 条、§2.3 出口条；`13` §5 注；`20` v2 改动清单、§2.4、§3.1、§3.2、§3.3、§3.7、§3.10、§4.2、§4.3、§九；`21` §三·1 续（序 6b、7b 与序 8、9、14 依赖）、§四·6（新步 7c、17c，步 7b、17a 指针）、§四·2、§七·1。未改 Rust 代码。

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B83 | 账本头行 `calib_used` 是本趟实际命中的记录集合：`Interp::run` 入口比对后清空、本趟按当前视图重填；续接时已命中键换线报 `W-header: calib_used_hash` 并列出变化的键，不拒绝；续接趟出口全按本趟线切，续接后账本只凭账本重放复现续接趟；账本格式不变。采纳（拒绝续接、按条目或按跳记版本、追加记录按位置取用、钉旧线四种候选都不采） | `12` §2.10、§3 J-18；`13` §5；`20` v2 §3.7、§4.2、§4.3、§九；`21` 新步 7c（`interp/outcome.rs` 一处清空加报文列键） | 已裁，未实现（步 7c，S 小，运行时轨，与 17a 不撞文件）；步 18 逐行落盘时 `calib_used` 须离开头行，载体交步 18 与 `Intent` 一起定 | 「按趟分段记」有混线与历史保留两种读法，分开裁；候选 (2)(3) 的问题在语义不在格式；7b 的哈希证明的是「头行记的线 = 重放用线」，头行记错时照样相等 〔B124 修订：载体定为 `Entry::CalibUsed { key, hash, record }`，18a〕 |
| B84 | 做值级来源：标量叶子与 Mat、Question、State 带来源标签 `(taint, sources)`，sources 为直接来源读数键集合；与 B33 共用同一标签、同一处 join、同一张边界表，taint 不是 sources 的投影而是第二分量，`derived_from` 是 sources 的 q_hash 投影；两分量只在「按计算键取下标或字段」一行不同（sources 并入键的 sources，taint 不并）；控制流不传播；`pick`/`at` 臂不改语言面、k 带 {出口键} 并继承出口 taint；谱系放行读 sources 经账本 `parents` 传递闭包；落新步 17c 紧接 17a、先于 12e-2，17b 依赖 17c。采纳（`pick` 臂交出结构值不采） | `12` §2.11 第 1 条注、第 9 条，§2.3 出口条；`20` v2 §3.1、§3.2、§3.3、§3.10、§2.4、§3.7；`21` 新步 17c、序 7b、8、9、14 依赖 | 已裁，未实现（步 17c，S，运行时轨）；17a 照常作为子集先落；步 18 的 `output_mat` 序列化 sources、删 `derived_from` | B33「值带来源链：无消费者」与同日 B59 未对照，本条合并；主会话「taint 作为投影」更正为同一标签两分量；17a 主会话指示以为 `Question.from_key` 有结构生产者，实为零；`pick`/`at` 的 k 恒 Trusted 与 §3.10 不一致，一并修 |

## 一·续七、标注门槛裁定进入依据文本（B85–B91，2026-09-24）

来源 `附注/2026-09-24-标注门槛裁定.md`（对 `评估/2026-09-24-新题标注门槛-办法.md` §五候选 C1–C7 的独立裁定；裁定者未参与该研究与校准轨施工；含 T1/S4R 独立复算与两组合成数据检验，脚本与输出在 `评估/2026-09-24-新题标注门槛-对照/裁定复算/`）。依 §5.6 授权写入，Nature 可点名否决。六项在该附注各条内自含。办法 §五的两个「只有 Nature 能答」的问题由主会话按 Nature 已表达的意图处理并裁入：(a) 上手两档都保留（试用约 32 条可路由、正式约 60 条可放行）；(b) 复核覆盖认证集的模型标注可作正式线真值（B36 授权的统计形式，保证相对于复核基准）——Nature 若要求正式线以人为基准，否决 B89 的全覆盖一支即可，其余不受影响。写入：`12` §2.3（B24、B72、B68、B63、B19/B36 条各一句，新 B85–B91 六条）与 §5 校准库行；`20` v2 改动清单、§2.3 `jpp-calib`、§3.4、§3.8、§4.4；`21` §三·1 续（序 4b/4c/4d，序 17、18、19 备注）、§四·5 I-8 行、§四·6 步 20d 拆分与 27a、新 §四·8（步 20g、20h、20i、20d-2 追加、20a 追加）、§六·3 V7。未改 Rust 代码。

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B85 | 拆分认证的分半改为规范序下正、负例段各自交替进两半，`seed` 只定起点；证书 `split-stratified`；不作缺省，只作 B86 的退路与对照臂。修改后采纳 | `12` §2.3 B24 条句与新条；`20` v2 §3.8；`21` 步 20g | 已裁，未实现（20g） | 研究者写「C2 不采纳则单独落步」，改为与 20g 同落 |
| B86 | 缺省认证方式改为固定序检验、不拆分：候选只由读数按计数生成，按从严到宽逐个以二项上界 ≤ α 检验、第一次不过即停，两侧通过前缀里取已决最多的合法对；每侧族错误率 ≤ δ 与拆分同级（推导在附注 §一·2）；正式线字面 60 / 语义 60–80 条、试用 32 条（裁定者复算一致）；候选规则进证书供 `load` 重跑；K 元同形。修改后采纳 | `12` §2.3 B24 条、B72 条数字、B63 条、新条，§5；`20` v2 §2.3、§3.8、§4.4；`21` 新步 20g、V7、I-8、27a | 已裁，未实现（20g）；复算：T1 正式 60 / 试用 32，S4R 60 / 32（拆分 160 / 60、无 / 100）；合成数据固定序假认证 1.8%–2.4% ≤ δ | 事前预测「同批不校正 n = 60 时 > δ」落空（4.5%）：嵌套阈值族上多重比较膨胀只约两倍；拆分省掉的样本多数买的是「保证有证明」不是「防实际过拟合」。研究者把候选规则当实现细节，改为进证书 |
| B87 | 加序贯 e 过程认证：按批导入、每候选混合 e 过程、E ≥ 1/δ 拒绝、每批按固定序过前缀；候选只在其已决集样本以均匀随机顺序到达时才可序贯判定（两端先标时非首组候选须标完再判）；停止规则缺省 `settled`（与同池固定序同覆盖），`--coverage-target` 可少标；覆盖由读数免费算，不影响放行。修改后采纳 | `12` §2.3 新条；`20` v2 §2.3、§3.8、§4.4；`21` 新步 20h | 已实现（20h，`39843846`，按主会话方案 A）；**修订（2026-09-25，`附注/2026-09-24-B104裁定.md` §三）**：候选取 B86 序列中已决数 ≥ seq_first 者、`random` 到达顺序须与标签无关（现行按 (p, 真值) 规范序置换在并列读数上失效，200 个种子里 64 个假认证率 > δ）、「settled 与固定序同线对 / 不宽于」改为各自有效、试用门槛 9；随步 20h-1；合成反例：两端先标按部分标注判使总体风险 0.25 的候选 100% 被认证，随机序 0.1% | 研究者的分层臂靠 `pick_pair` 平局规则偶然守住有效性，本条写成规则与测试；「首对停覆盖 50–75%」是 τ = 0 的定义不是缺陷 |
| B88 | `--from-ledger` 从首跑账本取读数作抽样框、按候选分组输出待标清单；清单不带读数与出口；组内随机、组间两端向内；回填按 `item` 回接。修改后采纳 | `12` §2.3 新条；`20` v2 §4.4 第 1 条；`21` 步 20h | 已实现（20h，`39843846`）；**修订（2026-09-25，B107）**：框与回接按 `(item, q)`、清单带 `q`、`E-list-ambiguous`，随新步 20h-2 | 研究者清单行带 `p`，标注者见读数会向读数靠拢（放行方向），删 |
| B89 | 模型标注记录写 `alpha_eff = alpha + (1 − a_lb(A))`，a_lb 只按落在已决区的复核行算；复核覆盖认证已决区时 `alpha_eff = alpha` 可上正式线（保证相对于复核基准，记录写 `truth_baseline`）；旧记录按 (1 − a_lb)/c；等级按 `alpha_eff`；复核文件带读数拒收；S4R 记录降 `Trial`，迁移为 80 条 Fable 盲复核集重认。修改后采纳 | `12` §2.3 B19/B36 条、B72 条、新条；`20` v2 §3.4、§3.8、§4.4 第 2 条；`21` 新步 20i、20a 依赖 | 已实现（20i）；S4R 迁移后正式上岗（`alpha_eff = α`）；**解读改判（2026-09-25）**：alpha_eff 超过试用 α 的等级为 `Provisional` 不是 `Trial`，路由不变，随 20c | 研究者把整体 (1 − a_lb) 当 A 上的分歧界，漏了条件化与置信合并；裁定者事前以为全覆盖要复核整个已决区（约 370 行），实际 B86 后认证集可为 80 行子集 |
| B90 | 27a 首批五条按题类与槽形覆盖选（五个题类、三种槽形、≥ 3 条真值可算、≥ 1 条语义走 B89、含 K 元与关系题式），九题类分批；每条按 B86 约 60 条；探针与试写只测落点率。修改后采纳 | `21` §三·1 续序 19、§四·6 步 27a | 首批已做（`评估/27a-准备/`，五条正式线）；**推翻条件已触发（落点 0/14）、B118（2026-09-25）改判**：第二批起按谓词级复用证据选，首批不重选、状态「已认证·未复用」 | 研究者 C6 的「九类各一加二」≥ 11 条与 27a 五条冲突，改为分批 |
| B91 | 范围扩展改为按 α 分档的认证：只验已有线对，每侧 n_needed(α) 零错即并入 `scope.extensions`，等级随所过 α；10 条复核只作外延探测、不升等级。采纳；核实为依据文本的规格缺口（B24 补充、B68），代码无 `scope_out` 清除路径，不是现行漏洞，随 20d-2 落 | `12` §2.3 B24 条、B68 条、新条；`20` v2 §3.4、§3.8、§4.4 第 8 条；`21` 步 20d 拆分注、§四·8 20d-2 追加 | 已裁，未实现（20d-2） | 研究者称「放行侧缺口」未分文本与代码 |

## 一·续八、仪表读数 4 诊断裁定进入依据文本（B92–B97，2026-09-24）

来源 `评估/2026-09-24-仪表读数4诊断与裁定.md`（第一次 T1 读数 4.97× 的拆解；裁定者未参与探针、基线、仪表、步 31-1 与非设计者程序）。依 §5.6 授权写入，Nature 可点名否决。六项在该文件 §八各条内自含。写入：`12` §2.2 刷新点句注、§3 J-02、J-05、J-07 注；`20` v2 改动清单、§2.3、§2.5、§3.1、§3.6、§3.10、§4.3、§五 S3；`21` §三·1 续（序 5c、20 组内、21b、22b）、§四·7 步 31-1 注、新节 §四·9；`17` 验收 1 注、里程碑 ②③、阶段 6。落位按 Nature 2026-09-24 全局顺序（先收口设计、再按 `21` 施工、最后验收），不因读数插队。未改 Rust 代码。

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B92 | J-02 的「派生」只指值依赖：sources 的每条边带种类（值依赖 / 选择依赖），`derived_from` 只取值依赖边的 q_hash 投影；`sieve`/`pair` 元素的 `item`、`over[k]`、按出口做的 `filter` 是选择依赖，再问同一题是账本命中不是自指；跳数与谱系放行计全部边；同键两种边并存按值边计。采纳 | `12` §3 J-02 注；`20` v2 §3.1、§3.10 三行；`21` 步 18（`output_mat` 序列化 sources 带种类、删 `derived_from`）；`tests/bypass/b92_derivation.rs` | 已裁，未实现（步 18，序 15）；17c 的临时做法（`derived_from` 独立传播）到步 18 结束 | 17c 把「选择边算不算派生」当实现细节延后；本条明确三个消费者需要三种投影 |
| B93 | 预算停机在刷新点降级：`flush` 按登记顺序发到预算为止，其余组按缺席处置（`absent_marks = budget`、记缺席账、不发），`cut` 给 `Unsure(budget)`，程序照常返回并报 `budget.exhausted` 与 `W-budget`；预算耗尽不产生 `Halt`，`Pending` 只留给 `ask`；`sieve` 的 `Halt` 特判与入口预刷新删除；效应站点超预算产出 `Fail { kind: Budget }`。采纳 | `12` §3 J-07 注（「停」指停发）；`20` v2 §4.3 半句改写；`21` 步 22 第一子项 22-0（序 20）；`tests/bypass/b93_budget_degrade.rs` | 已裁，未实现（22-0）；align 两份 T1 版、folio 策略 A 的预算停机在此之后可过 | 31-1 写成「`sieve` 之外没有降级」，实际是首跑路径没走已有的缺席处置；「一个都发不了 → Halt」是 `20` v2 §4.3 原文，改 |
| B94 | `cut` 惰性：出口在第一次检视（`handle`、`if`/`match`、读出口的内置、`consume`、宿主读内容、返回）时解析，刷新点随之移到检视点，登记、融合、分层规则不变；`lift` 按 13b 的三个条件穿过直线段里的用户函数调用，同一状态的多次 `judge` 提升到段首；递归逐题按构造串行，不在保证范围。采纳 | `12` §2.2 刷新点句注；`20` v2 §2.5 批调度行加两条「无意绕过的写法」；`21` 新步 23c（序 20，规划轨 + 运行时轨）；`equiv_pairs` 加 `seq_wrapped`；`tests/bypass/b94_lazy_cut.rs`、`b94_lift_calls.rs` | 已裁，未实现（23c）；预注册 winnow 设计者 T1 版 4 层 → 2、align 非设计者 T0 版 36 次 → 9、folio 非设计者版 13 → 13 | 31-1 把「同层两次 `sieve`」与「逐题 `judge`」分开报，本条合为「刷新点在哪」一个问题；winnow 设计者版注释写的是意图不是实现 |
| B95 | 丢弃收窄：`consume(x, "drop")` 不收契约值；缺席类原因（`budget`、`absent`、`latency`）的出口不能 drop（`E-drop-unobserved`）；J-05 报文与 `check --json` 的 `fix` 按上下文先给转交写法，drop 排最后并带条件；返回前可达性核加 `W-drop-then-return`。采纳（删 drop、带理由字符串、自动挂到返回值三种候选不采） | `12` §3 J-05 注；`21` 步 21（运行时半：`duty.rs`、`b_consume`、J-05 报文）与步 24（静态半：`W-pending-unreturned`）；`tests/bypass/b95_drop.rs` | 已裁，未实现（21、24，序 20） | 31-1 记为文档问题；本条更正为语言问题的一半：文档让转交被看见，代价差由语言定 |
| B96 | T1 (d) 改性质验收：任务书只写性质（每侧 CP 上界 ≤ α、n ≥ 22、样本不足待真值、方法自定、只写本项目有的题型），验收 ① 由考官在程序看不见的留出集上核，③ 由标签自检保证任何过 ① 的线给同一出口；旧五份 T1 基线改标 `t1-strict` 只报不判；T1 每侧两份实现；仪表并列「全部实现」与「只计验收全过」，位置判定用后者，加「J++ 侧无通过实现的项目数」一格；(c) 上限按「每层每份材料至多一次」写回 B79；看到任一侧输出后改任务书须记触发来源并由独立脚本复算期望。修改后采纳 31-1 的偏离 | `21` 新步 31-1b（序 5c，测试轨）；`17` 验收 1 注与里程碑 ②③；`附注/2026-09-24-仪表读数1裁定.md` §三·3 (c)(d) 加注；`probes/_baseline/任务书-通用.md`、`make_labels_t1.py`、`t1_check.py`、`measure_expr.py`、`dashboard.py` | 已裁，未实现（31-1b）；预注册 T1 折行归一中位 4.97 → 全部实现 3.8–4.4、只计通过 3.2–3.7 | B79 估 (d) 40–60 行按性质估，31-1 按逐位对上写任务书，两者没对齐；4.97 里约 1.0–1.6 来自测量 |
| B97 | 写程序代理的配备：签名、形状、示例、诊断说明与修法由代码里两张表（`BuiltinSpec`/`ConstructSpec`/`EffectSpec`，诊断编号表）经 `jpp describe`、`jpp explain`、`check --json` 的 `fix` 生成；skill `writing-jpp` 正文只写为什么（每条标 `00`/`12` 条款号）、固定流程、写的顺序、禁则、验收，签名经动态注入或 CI 生成的引用文件进入；CI 比对生成快照、跑全部文档代码块与 skill 命令、核对条款号存在、冷线跑全部程序；不做 MCP；用 31-1 的测法两臂验证（31-3）。采纳 | `20` v2 §2.3 `jpp-check`、`jpp::cli`，§五 S3；`21` 新步 9b（序 21b）、31-3（序 22b）；`17` 阶段 6；`研究/09-代理技能与提示写法-官方指引核实.md`（skill 格式与写法的一手依据） | 已裁，未实现（9b、31-3）；31-1 冻结的七份非设计者版随 31-3 重派；预注册非设计者版 / 设计者版行数比 1.8–2.9 → 1.0–1.4、drop 4/5 → 0/5、T1 → 5.5–6.2 | 31-1 归为「文档轨」；本条更正为 crates 改动为主，文档是它的输出 |

B81 (c) 核对（同文件 §六，不触发推翻）：聚合构造（`tally`、`first_k`、`iterate`）的 `pending` 是聚合出口本身，返回输入契约值的条目不覆盖它，程序读了聚合的 `value` 就须显式转交它的未决；B81 §四·3 的 refund 改写示例删了 `count.pending` 是示例错，预注册 refund −4 改 −3；配 `W-pending-unreturned`（步 24）与 `cold_run.py`（31-1b）。

## 一·续（设计收口盘点）、B98–B103 进入依据文本（2026-09-24）

来源 `评估/2026-09-24-设计收口盘点.md`（第 0 轮设计收口闸门：49 条「只做过实验未定」分诊、落步表 67 条、两个原型分支去向、设计完整性结论；裁定者未参与当日施工与仪表测量）。B92–B97 由同日 `评估/2026-09-24-仪表读数4诊断与裁定.md` 取用，本节接 B98。依 §5.6 授权写入，Nature 可点名否决。六项在该文件 §一·3、§三·4、§五·3 各条内自含；写入依据文本的句子带「依 Nature 2026-09-23 授权，Fable 裁定 Bn（地基/评估/2026-09-24-设计收口盘点.md）」。

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B98 | 宪法借用登记表加三行：函数式组合子（组合子只在契约值上定义，三原语加骨架，`bind` 由未决续接承担）、部分求值与常量传播（只限 J-06 `bound` 与 `budget` 下界，不做一般部分求值）、Lisp 宏展开 / 分阶段程序（展开是效应须记账，程序作为值本版不补） | `00-宪法` 末节「登记行增补」加三行 | 已写入；总账 L-057 改已定未造（归 L-056，步 25），P-020、K-233 改已造出，K-106、K-116、L-058 改被取代 | 无 |
| B99 | J++ 本版不加「目标」构造；范围落 `over` 与料库范围（B20）、比较依据落 `ref` 与校准键（J-04）、完成要求落 `budget` 与迭代终止线（B43），各只声明一次；执行策略由聚合子与骨架签名承载；影响执行的文字必须进 state 或题式（J-11） | `12` §2.7 宿主段加一句 | 已写入；总账 L-064、G-049 改已定未造（归 K-228，步 25、29），K-229、G-051、K-217 目标字段、K-109 目标驱动改被取代 | 底稿把「目标」当候选机制，本条定为落点问题 |
| B100 | `12` §7 能做域表：加「人作为执行器」行判域外（AUC 0.541）；「枚举器」行方向定为把输入削到判断器判得动的形状（B51-R2 `mat_shape` 的静态消费者），不是补判断器写不出的任务 | `12` §7 表加一行、改一行；附注拟增行转正 | 已写入；总账 G-011、G-058 改已定未造（文档项，随写入完成） | B51 曾对 G-058「方向待实测不裁」，本条按 B51-R2 已定语义定方向，实测只用于推翻 |
| B101 | J-10 静态面：求和范围是题构造站点；未知或非字面键站点按 1 计并报站点数；循环或函数体内站点按可能多遍计且诊断明写「不是上界」，Σ ≤ limit 时有此类站点也报 W 级提示；前端接受 `budget{unsure}` | `12` §3 J-10 行加一句；`21` 步 24 | 已写入；代码前两条已在 `check_with_calib`；总账 K-185 改已定未造（步 24），G-012 前端一半同落步 24 | 原为总控提议未经 Nature 认，以 §5.6 授权转正 |
| B102 | `ask` 与 `cut(judge(…))` 同产 `Exit`，可替换性由类型承担；不给 `ask` 加可编程处理方，不解绑 trusted 与真值资格；程序答题走 `judge`/`gen`（B18）；换判断器实例替代人是 B60 路由问题不裁 | `12` §2.6 加一句 | 已写入；总账 K-225 改被取代 | 无 |
| B103 | 验收 2「逐跳一致率」= 第 h 跳已决出口与真值一致的比例（真值按 B19/B36 取序，经 B88 清单回接账本；无真值的跳报无数）；「弃权率」= 第 h 跳 `Unsure` 占比，`budget`/`unobserved` 单列；两条曲线只在真机账本上出，固定观察只出跳数分布 | `00-目标与动机` §六第 2 条加一注；`21` 步 31 深度曲线取数 | 已写入；总账 G-003 加注 | 无 |

同一盘点的非裁定结论（不占 B 号）：49 条「只做过实验未定」全部分诊（16 改已定未造、4 改已造出、29 改被取代）；落步表 67 条「找不到」逐条落步或改桶；`21` 新步 25b（多能力组合程序）、30b（通爻式发现程序）由「不占工程步」改为占步；实验 V8（裂变近似性）挂步 23b；六项待裁增补 A-1、A-3b、A-4、A-5、A-7、A-12 建议在 14a 后一次裁完。

## 一·续（B104 与 B87 修订）进入依据文本（2026-09-25）

来源 `附注/2026-09-24-B104裁定.md`（步 20g-1 末尾交 Fable 的两问：δ 错配的分法、无文本追加导入后的范围指纹；主会话追加的 B87 方案 A 确认与试用门槛复算；裁定者未参与 20g、20g-1、20h 施工与 PR #31 评审）。依 §5.6 授权写入，Nature 可点名否决。六项在该附注 §一、§二、§三各条内自含；含一组可证伪的模拟检验（`评估/2026-09-24-新题标注门槛-对照/裁定复算/seq_order_check.py`，预测写在运行前）。写入：`12` §2.2 画像条、§2.3 B68 条、B87 条、新 B104 条；`20` v2 改动清单、§3.4、§3.8、§3.9、§4.4；`21` §三·1 续序 4c-1、§四·8 新步 20h-1 与 20c、15d 追加。未改 Rust 代码。

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B104 | (1) `cut` 的判区带宽不小于证书记录的认证带宽：加载画像取 max(δ_证书, δ_运行)，未加载画像取 δ_证书，不等报 `W-delta-mismatch`（推导：判区 ⊆ 已检验区 ⟺ δ_用 ≥ δ_证书，两支都满足；未加载画像分支就是 `20` §3.9「桥的 δ 只从记录取」的目标形态，与 B73 一致）；没有记录 δ 的旧证书不作放行凭据，路由不变、报 `W-delta-unknown`，由 20c `load` 重跑复现 hi / lo 后写回 δ。(2) 范围指纹是放行凭据：无指纹 = 范围未知 = 路由不变、不放行（`W-scope-unknown`），B68 之前旧记录同此，20d-1 的「缺指纹按范围内」宽限撤销；认证集部分带文本时用带文本样本写指纹并记 `n_text / n`（子集区间 ⊆ 全集区间，只多判范围外不多放行）；不要求补齐文本才能认证。采纳 20g-1 的 δ 分法，修改后采纳「有文本部分写较窄指纹」 | `12` §2.2 画像条一句、§2.3 B68 条两句、B91 条后新条；`20` v2 §3.4 条件表两行与 `releases()` 两项、§3.8、§3.9、§4.4 第 4 条；`21` 新步 20h-1（序 4c-1）、20c 与 15d 各追加一句 | 已裁；δ 两支已实现（20g-1，`3d9ee2a4`）；旧证书位、范围未知位、子集指纹未实现（20h-1）；预注册：三个 `@truth` 金样报告 `releases` 变 `false`、各多 `W-delta-unknown` 与 `W-scope-unknown`，`value` 与账本不变；仓库内无指纹、无 δ 的记录只有 `_calib/b24` 四条，真机 V7 记录不受影响；**2026-09-25 确认与修订**：「δ 未知」= 证书带 `selection` 无 `delta`（20h-1 解读确认，生产者不变量随 20a-2）；20c 旧证书的 δ 由样本与线解出、不取当前画像（B117） | 20g-1 把「无画像按证书 δ」当暂行修法，实际是目标形态，暂行的是 max 分支；「旧证书沿用运行期 δ 不报」被当作保守，实际是唯一没封的口；「部分带文本不写指纹」是 20d-1 的施工选择不是 B68 条文，情形「有指纹记录追加无文本批」的退步自 20d-1 起就有 〔B122 修订：「hi = h − δ 只有一个解」只在规则不依赖 δ 时成立；多解降 `Fixture`〕 |
| B87 修订 | 主会话方案 A 确认（每候选只按混合 E 判、settled = 首个未过候选定局；方案 B 两次检验 2δ 否决正确），修正三处：候选取 B86 序列中已决数 ≥ seq_first(α, w) 者而不是平移格（零错全标时才与固定序同线对）；`--order random` 到达顺序必须与标签无关——按 (p, item) 排序后置换，现行按 (p, 真值) 规范序置换在并列读数上失效（模拟：并列块 36 条、r = α、200 个种子里 64 个假认证率 > δ，最高 0.857；与标签无关的置换 0.080）；「settled 线对与同池固定序相同 / 不宽于固定序」删去，改为各自每侧 δ 有效、零错全标时相同、一般情形可宽可严；试用档零错门槛 9（= n_needed），不是 10。修改后确认 | `12` §2.3 B87 条四处；`20` v2 §3.8、§4.4 第 3 条；`21` 步 20h 施工订正加指针、步 20h-1 第 3 项 | 已实现（20h，`39843846`，方案 A）；三项修正未实现（20h-1）；`random` 顺序一项是合入代码里的放行方向缺陷，今天仓库内无序贯认证的记录 | 原条把零错情形当一般性质，候选格在 seq_first 处对不上；原条没查实现里的「随机」是否真与标签无关（规范序含真值在 B85 分半里无害，在到达顺序上有害）；裁定者事前把 e 过程判为死重，核对 JEV 读数高度并列（80 条只有 5 个值）后更正——并列扩边使首候选整块 36 条、e 过程 24 条零错即拒，序贯确有收益 |

## 一·续（B105–B106）进入依据文本（2026-09-25）

来源 `附注/2026-09-25-B105-B106裁定.md`（步 14b-0 末尾留给后续步的三条与施工代理提的三个选项：入口绑定成 `Mat` 还是读出值、`Program.entry` 的声明语法、`purpose` 的去向；裁定者未参与 14b-0 施工）。依 §5.6 授权写入，Nature 可点名否决。六项在该附注 §一、§二、§三各条内自含。写入：`12` §2.7 新段、§2.11 B33 第 5 与第 8 条各一注；`20` v2 改动清单、§2.3、§3.2、§3.10、§七；`21` §三·1 续序 11、§三·6 步 14b、§四·4 A-13。未改 Rust 代码。

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B105 | 入口参数分值条目（CLI `--input`；读出值绑定、叶子 taint 宿主声明缺省 Untrusted、sources ∅、无 origin，程序直接 `input.x`）与材料条目（`Value::Mat`，运行时盖 `origin = ["input"]` 并清来源键，taint 宿主声明缺省 Untrusted）；`mat(input.x)` 仍按 B33 第 5 条记 `computed`，`Provenance` 不加第三分量，`Mat` 不加隐式字段读取；`entry_hash` 改为整份入口哈希（`purpose`、各条目种类 / 名字 / 内容哈希 / taint），一份算法；`purpose` 有则绑名 `purpose`（Text、Untrusted）并进哈希。施工代理 (A) 不采（`Origin::Input` 的落点只在整份判时出现，11 份探针无一如此，代价是每份程序多一行仪式，T1 只计全过约 3.69× → 3.58×）、(C) 放弃（无规则消费者）、(B) 修改后采纳 | `12` §2.7 新段、B33 第 5 条与第 8 条注；`20` v2 §2.3 `EntryArgs` 句、§3.2 一句、§3.10 两行、§七一行；`21` 步 14b | 已实现（14b）；**修订（2026-09-25，B108）**：CLI 可带 `--input-trusted`，「不提供声明开关」一句撤回，随新步 14b-1；预注册：金样逐字节不变，11 份探针不改，带 `--input` 的 `entry_hash` 字节值变一次（仓库内无记录依赖） | 14b-0 与施工代理把「`Origin::Input` 无落点」当作绑材料的理由；落点由程序是否整份判决定，不由绑定形式决定 |
| B106 | `Program.entry: EntryDecl`（名字、种类、taint）由 `Session::compile` 从宿主名字表写入（唯一写入处），serde 缺省、空不序列化、不进哈希与金样；检查器名字趟改读它，`check_with_host` 与 `Checker.host_names` 删；源码声明语法本步不定，登记 A-13（与 A-12 同裁、步 24 前；触发：形状标注进文法、探针需程序自描述、J-08 静态面需要）。默认做法采纳 | `20` v2 §2.3 `Program.entry` 注释与 `compile` 签名；`21` 步 14b、§四·4 A-13 | 已裁；未实现（步 14b） | 14b-0 把 IR 字段读成「必须由源码声明」；字段与来源分开，宿主名字表与源码声明是同一字段的两种生产者 |

## 一·续（B107–B120）进入依据文本（2026-09-25）

来源 `附注/2026-09-25-批量裁定.md`（阶段评估 ② 候选裁定 B107–B109 的独立复核；27a 准备后主会话追加的三项（B118–B120）；`21` §四·4 六项待裁增补 A-1、A-3b、A-4、A-5、A-7、A-12/A-13；三条施工中的解读（B104「δ 未知」、B89 alpha_eff 超试用 α、题级记录到 B30 元组的映射）；主会话追加的 20c `load` 复现判据。裁定者未参与评估 ② 与 14b、15a、20a-1、20h-1、20i 的施工）。依 §5.6 授权写入，Nature 可点名否决。六项在该附注各条内自含。写入：`12` §2.2 B30、B34 条，§2.3 B88、B89、B104 条，§2.7 B105 段，§2.12 B17 条，§3 J-05 注、J-06 行、J-08 行、B22 总述，§5 骨架行；`20` v2 改动清单、§2.3、§2.4、§3.4、§3.6、§3.7、§3.8、§4.4；`21` §三·1 续（序 1c、4e、11b、11c、里程碑 ③ 行）、§三·6、§三·8、§三·9、§四·4、§四·8、§四·9、新节 §四·10；`17` 阶段评估条。未改 Rust 代码。

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B107 | 待标清单的抽样框与回接按 `(item, q)`（不再按状态去重），清单行带 `q`；标签行无 `q` 而同 `item` 在该键下多读数报 `E-list-ambiguous`，不合并；报告加 `questions` 表，`--list-out --report` 给清单附题面与填法；K 元框同形；20a-2 后从账本 `calib_ref` 取题面、`--report` 撤。修改后采纳（`fill` 来源改为报告，账本本步不改格式） | `12` §2.3 B88 条；`20` v2 §4.4 第 1 条；`21` 新步 20h-2（序 4e） | 已裁，未实现；**放行方向，现在插步**；`README`「边标边导」先加警示；`评估/2026-09-25-试写/aspects/calib-list` 的记录标不得装载 | 20h 与 B88 条文都按单题写 `item`，条文与实现同错；评估者假定账本记填法，实际不记 |
| B108 | CLI `--input <file>` 可配 `--input-trusted`（整份声明，不加字段级），声明进 `entry_hash`（已含）、报告加 `entry` 段；B105「CLI 不提供声明开关」一句撤回；J-08 静态子面「守卫全不可信」（每个合取项都确定来自不可信状态且无 `ask`；知动作表且不可逆为 `J-08`、否则 `W-guard-untrusted`；追不到来源按可信计）单独成步、不等步 24。采纳 | `12` §2.7 B105 段、§3 J-08 行；`20` v2 §2.3、§2.4；`21` 新步 14b-1（序 11b）、24-0（序 11c） | 已裁，未实现 | B105 把「宿主」读成嵌入的 Rust 调用者，漏了 CLI 操作者；评估者把静态子面写成要等 15 段，实际不依赖它 〔B128 同构（2026-09-25）：`--release-on-declared` / `EntryArgs.accept` 与本条同法进 `entry_hash`、报告 `entry` 段〕 |
| B109 | 里程碑 ③ 门槛改为计数门槛：27a 五条各有线与真机已决率读数、其中放行等级 ≥ 3 条；深度曲线 ≥ 2 跳且在骨架程序上；`select` 真机 ≥ 1 `Pick`；能力对照 ≥ 8/12；第八项有数；步 25 骨架真机各一次重放 0 调用；T1 多实现中位不设线。「已决出口率 ≥ 0.9」不采。第一份真机深度曲线现在出（31-0c）。修改后采纳 | `17` 阶段评估条；`21` 里程碑 ③ 行、新步 31-0c（序 1c） | 已裁，未实现（31-0c 预注册先提交，≤ 0.001 美元） | 评估者把判断器与题面的属性（0.9）写进门槛，改为只随工程推进变的计数 〔B123 订正：refund 只有 `cs-refund` 真值；③ (2) 加逐跳 B19 真值前提〕 |
| B110 | A-1：契约值加一等字段 `ignored`（已决否定集合），与 `value`、`pending` 并列为三值出口的三个分区；`contract::build` 必收（可为空、不可缺省，缺即 `E-outcome-missing-ignored`）；`detail.ignore` 撤；`tally.all` 读它；`E-tally-missing-rejected` 退役。采纳 | `12` §2.12 B17 条；`20` v2 §3.6；`21` 步 25 | 已裁，未实现（步 25） | 无 |
| B111 | A-3b：`bound` 实参是函数参数时，J-06 静态面在每个调用点核实参为字面量或 B69 常量、否则在调用点报 `W-bound`，体内不报，沿「参数 ← 调用点实参」逐层向外、递归不传播；骨架度量与静态下降证明本版不做（已定未造，不占步）。采纳一半 | `12` §3 J-06 行；`21` 步 24 补项 | 已裁，未实现（步 24） | 无 |
| B112 | A-4：契约值不带情景树字段；树是账本 `site`/`parents`/`hop`/`layer` 与元素 `trail` 的派生视图（`jpp explain --tree`，随 9b）；作者写的 `detail.tree` 只是标签。不采字段形式 | `12` §5 骨架行；`20` v2 §3.6；`21` 步 25、9b | 已裁 | 无 |
| B113 | A-5：`12` §5 骨架行补三处——未决支是 J-05 去向的落点、终止论证须覆盖细化支；`gen`/`do`/`transform` 是陈述节点、`ask` 是人答的题节点、终止论证不得依赖效应输出；预算停机是树外截断、不入终止论证。采纳（盘点建议方向加精确化） | `12` §5 骨架行；`21` 步 25 | 已裁 | 无 |
| B114 | A-7：不为 B22 八类加静态规则；封闭的静态面由 J-01 的类型面承担（`Reading` 是句柄，只有 `cut`、`fit`、`repeat` 收它）；第九类先加语言形式再加规则。不采 | `12` §3 B22 总述；`21` §四·4 | 已裁，无步 | 无 |
| B115 | A-12：`W-untyped-transfer` 只对具名函数报，函数字面量的未决照现行交给外层帧、不报；返回类型标注不进文法。A-13：本版不进文法，触发加第四条（9b/`cold_run` 需不带 `--input` 的 `check`），形状预定 `entry <名>: value \| mat`、taint 永不在源码。A-12 改判、A-13 不采 | `12` §3 J-05 注；`21` 步 21 追加、§四·4 | 已裁，未实现（步 21 追加） | 探针 I-7 把规则错套写成文档摩擦 |
| B104、B89 解读 | (a) 「δ 未知」= 证书带 `selection` 而缺 `delta`，`certify` 线与代价线不置位，`selection: None` 只许这两个生产者写出（不变量随 20a-2 加测试）——确认；(b) alpha_eff 超过试用 α 的等级是 `Provisional`（路由、不放行），不是 `Trial`、不是冷——改判，路由不变 | `12` §2.3 B104 条、B89 条；`20` v2 §3.4 | 已裁；(b) 随 20c，今天无金样受影响 | 20i 因 `Provisional` 当时放行而取 `Trial`，20a-1 修后回到 B89 原文 |
| B116 | 题级记录映射（20a-2 前置）：手写题 = 零槽题式，与题式记录同一键空间，`q_text_hash` 只作单填法覆盖；作者串只是类标签；带 `form`/`question` 的行产主键记录，只带 `key` 的行按 `class: key`（`W-key-as-class`），`--from-ledger` 从账本 `calib_ref.key`（20a-2 加）取主键，`put` 记录进夹具命名空间；查找链 覆盖 → 题式 → 类 → 夹具 → 冷；迁移带 `--questions` 映射（`jpp check --questions-out`）算零槽 `form_hash`，缺映射降类记录待真值，`slot_kinds` 缺记 `partial_key`。裁定 | `12` §2.2 B30、B34 条；`20` v2 §3.7 (1)、§3.8；`21` 步 20a-2 | 已裁，未实现（20a-2）；V7 两条作者串记录须带映射迁成主键记录、在 31-0c 之后 | B30 把「题级」写成独立一层，更正为零槽题式；20a-1 阻塞事实 2 由此消失 |
| B117 | 20c `load` 复现判据：(a) 只看决定出口与等级的量（每张证书的线、`alpha`、`conf_delta`、方法与参数、`eff.alpha_eff` 与试用位，记录 hi/lo），计数与 `e_*` 不入；(b) 复现即写回，同址替换、旧证书移入 `certs_history`，`unsure_rate` 重算；(c) 旧证书的 δ 由样本与线解出（上侧 δ = h − hi、下侧核 l + δ = lo、相等且 ≥ 0），不取当前画像；(d) 同批两侧与无过程方法降 `Fixture`。写回合 J-03（重跑即 `commission`、线不同不写、历史不删、只在 `load`）。(a)(b)(d) 确认、(c) 改判 | `12` §2.3 B104 条；`20` v2 §4.4 第 4 条；`21` 步 20c 追加 | 已裁；20c 施工中按此 | 主会话「当前 δ」在画像 δ 改变时会假降级，改为由线解出 〔B122 修订：(c) 的解 δ 是枚举加复现，恰一个复现才写回〕 |
| B118 | B90 推翻条件已触发（首批五条在 14 道题上落点 0），补救不采「题类内按使用频次」：第二批起按谓词级复用证据选（≥ 2 独立目的问过同一谓词，措辞归并由 Fable 复核写进预注册；证据来自探针、试写、示例与 B20 清单，判据是独立目的数）；首批五条不重选，作管道证明入库、状态「已认证·未复用」；落点率改在 31-3 臂 B 上测选用率（目标 ≥ 1/2）。修改后采纳 | `题库/规范.md` §三；`21` 步 27a；`19` B90 行 | 已裁；27a 第二批预注册前归并 | B90 推翻条件把原因归到题类频次，实测证明复用在谓词层；旧程序上的落点率混淆了「作者不知道题库」 |
| B119 | F2 保留、不改模板，口径「只指出答案在哪里查不算」写进条目说明「标注口径」栏（不进模板、不作前提声明）；推翻条件：带内率超认证集三倍或触发 5(c) 则拆分。Nature 的 B 栏授权覆盖题库规范（B48、B49 先例），按草案加四处修订定稿为 `地基/题库/规范.md` v2，`变更记录.md` 从原型并入并追加一条。采纳 | 新建 `地基/题库/规范.md`、`地基/题库/变更记录.md`；`21` 步 27a | 已定稿；Nature 可点名否决即回退为草案 | 无 |
| B120 | 三个补丁归属：(a) 记录 `kind` 取精化类——报告 `questions` 表加 `kind` 列、标签行可带 `kind`/`slot_shape`/`over_kind`、取序 行 > 报告/账本 > 基础类、矛盾 `E-kind-conflict`；(b) `exits` 表带 `index`/`pos`/`item` 摘要（`outcome.rs`，跨轨登记）；(c) 标注包编号不得携带生成者信息，写进规范 §1.3 与 GUIDE、不加代码检查。三条都落 20h-2 | `21` §四·10 步 20h-2；`题库/规范.md` §1.3 | 已裁，未实现（20h-2） | 无 |

## 一·续（B121）进入依据文本（2026-09-25）

来源 `附注/2026-09-25-B121守卫证据裁定.md`（步 16 施工代理提的两种读法与 A/B 实测；主会话追加的 unsure 臂放行缺陷 `ccd9fe03`、拆 let 多放行、`bypass_j08_guard_ev.rs` 12 条；运行时轨五条依据；裁定者未参与步 16、24-0 施工）。依 §5.6 授权写入，Nature 可点名否决。六项在该附注 §一、§二、§四各条内自含。写入：`12` §3 J-08 行；`20` v2 改动清单、§2.4 J-08 行、§3.1、§3.10 两行；`21` 步 16 改写、新步 16-0、§三·1 续两行、§四·4 A-14、新节 §四·11。未改 Rust 代码。

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B121 | 守卫证据 `GuardEv` 只在出口消去处产生，本语言的出口消去形式是 `handle` 分派：分派到臂的出口 e 以 `GuardEv::from_exit(e)` 压该臂守卫栈并盖进臂返回值的每个 `Bool` 叶子（递归 `List`/`Record`），`from_exit` 对 `Unsure` 恒空、否则 `(taint 可信 ∧ releases(), from_ask)`；`&&` 取并、`||` 与 `!` 清空、比较式与字面量为空、`if` 分支值不带条件证据、证据随值走不按名字记；`guard.rs::release` 唯一放行点；删 let 旁路表。臂里字面量不能伪造证据（`ignore` 臂返回 `true` 是作者分支逻辑，与今天 `ignore: fn(){do}` 同一放行面）；与 B33、B84、24-0 一致（静态报出 ⊆ 运行期拒绝）。拆 let、容器往返、`&&` 合取三种形状从拒改放是修正旁路表的假拒绝。现存缺陷：正式线可信材料切出 `Unsure` 时 unsure 臂里的 `do` 与臂返回的 `true` 都被放行——插热修步 16-0（`Exit::guard_trusted()` 加 `!is_unsure()`）。`len(xs) > 0` 一类比较式守卫按设计不放行、不加「从 sources 推证据」规则，GUIDE 写 `do` 进臂与见证出口两种写法；`||` 取交与 `any`/`all` 登记候选 A-14。(A) 不采、(B) 修改后采纳、施工代理 Unsure 修法采纳、(i) 采纳 (ii) 不采 | `12` §3 J-08 行；`20` v2 §3.1、§3.10、§2.4；`21` 步 16、新步 16-0、§三·1 续、§四·4、§四·11 | 已裁；16-0 现在插、未实现；步 16 未实现（运行时轨有 (B) 本地实现，702 过、金样不变）；预注册：金样逐字节不变，`bypass_j08_guard_ev.rs` j08_2、j08_8、j08_9 从拒改放 | `20` §3.1 把「读出」写成不存在的内置 `is_act`，施工代理据此把 (A) 当成可执行语义；`guard_trusted()` 的名字让三处折叠与 `守卫()` 都当它已含「已决」；24-0 追加把 triage-do 的守卫写成等步 16 修，实际按设计不放行 〔B125 修订：(f) 值臂只盖值不压栈；`release(name, reversible, sp)`；j08_19 为步 24 常设核对〕 |

## 一·续（B122–B127）进入依据文本（2026-09-25）

来源 `附注/2026-09-25-待补批量裁定-2.md`（`过程记录/总账待补条目.md` 全部标「待 Fable」的行：20c 旧证书 δ 解不唯一、B109 真值表述、步 18 拆分与四项格式问题、步 16 与 B121 字面出入、15g 两条降级的落点、校准键缺 `model`；另核对第 11、14、17、18、22、33、42、49、51、54、55、57 行已被 B33/B84、B71、B83、B92、B95、B116、批量裁定解读 (a)(b) 与本表覆盖。裁定者未参与步 16、18-0、20c、31-0c、15g 的施工）。依 §5.6 授权写入，Nature 可点名否决。六项在该附注各条内自含。写入：`12` §1.3 两行、§2.2 B60 条、§2.3 B104 条、§2.10 B83 条；`20` v2 改动清单、§4.4 第 4 条、§九账本行；`21` 步 16、18、20a-2、20c 追加、24、25、31-0c、§三·1 续 1c 与 12c 行、新节 §四·12；`17` 阶段评估条。未改 Rust 代码。

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B122 | 20c 旧证书缺 δ 时在读数网格上枚举候选并要求逐位复现，恰一个复现才写回，零个或多个都降 `Fixture` 并列出候选，`load` 不在多解里挑（不取最大、最小、画像值）；多解记录重新认证，迁移不给 δ 参数；「hi = h − δ 只有一个解」改为只在规则不依赖 δ 时成立。确认现行 | `12` §2.3 B104 条；`20` v2 §4.4 第 4 条；`21` 步 20c 追加、20a-2 | 已裁；20c 已按此实现（`probes/scope` 三条降级） | B104 / B117 把候选阈值 h 写成只由样本定，漏了规则按 δ 选 h |
| B123 | B109「`labels.jsonl` 两键都有真值」订正为只有 `cs-refund`；十行 Sonnet 盲标不进主读数、不复核（n = 10 达不到 B19）；③ (2) 加前提：深度曲线程序每跳材料落在 B19 真值集上（`computed` 或 27a 条目目录标注集），步 25 / 31 预注册逐跳写真值来源。修改后确认 31-0c | `17` 阶段评估条；`21` 步 31-0c、§三·1 续 1c 行 | 已裁；31-0c 记录已按此处理 | 裁定者写 B109 时未核标签文件的键 |
| B124 | 步 18 拆分（18-0 → 18c → 18a → 18b）与 Q1 (b) `Entry::CalibUsed` 逐键比、Q2 (a) `output_mat` 序列化 sources 删 `derived_from`、Q3 v2 迁移、Q4 18c 单独成步全部确认；18a 的 v3 为 20a-2 留位 `calib_ref.{key, kind, fill}`（`serde(default)`、为空不序列化），20a-2 只填值不改账本格式。裁定 | `12` §2.10 B83 条；`20` v2 §九账本行、改动清单；`21` 步 18 注、20a-2 | 已裁；18c 施工中，18a / 18b 未开 | B116、B120 把「账本 `calib_ref` 加字段」写成 20a-2 的事，未对照 ET1 |
| B125 | 步 16 与 B121 §五·1 三处出入接受为实现等价：值臂只盖值不压栈（(f) 字面改）；`release(name, reversible, sp)`；加测 4 条，j08_19 定为步 24 扩 J-08 静态面的常设核对。裁定 | `21` 步 16 | 已裁；步 16 已合入 | B121 (f) 把 `fn` 臂的压栈搬到不求值的值臂 |
| B126 | H1 / H8 降级不落 25b、29：H1 守卫半 = 规则 `E-modality`（步 24），渲染半 = S 库 `ocr` / `a11y` / `table` / `strip_assertions` 读画像、反值时为空操作（返回输入材料本身，不发效应、不记账；步 25 S 库声明行）；H8 守卫半即 J-08，不给 `state` 加自动去主张。裁定 | `12` §1.3 H1、H8 行；`21` 步 24 补项、步 25、§四·12 | 已裁，未实现（24、25）；15g `h1_*` / `h8_*` 在 24 / 25 后取消 ignore | `21` 起草时把 §1.3 降级表当作已被现有步覆盖，实际只有 H5 有落点 |
| B127 | 校准键 `model` 分量由 20a-2 落、15g (D2) 测试作门禁，既有记录 `model` 取来源账本头 `model_id`、缺则 `--model`，固定观察按账本头查线；过渡期 `--calib` 只对 `fixed` 与 `live` 开放，其他后端带 `--calib` 报 `E-calib-model`（随 15g 或 15g-1），20a-2 合入时删。裁定 | `12` §2.2 B60 条；`21` 步 20a-2、§四·12、§三·1 续 12c 行 | 已裁，未实现（15g / 15g-1、20a-2） | 15g 合入后 `stub` 可带 `--calib` 的空窗没人算 |

## 一·续（B128–B130）进入依据文本（2026-09-25，作者主权与策略表达）

来源 `地基/附注/2026-09-25-作者主权与策略表达裁定.md`，问题直接来自 Nature 2026-09-25 原话（「让我自己判断多少分算是要退款」「为什么剥夺我的策略」「帮我表达而不是改掉我的表达」），裁定以 `00-目标与动机` 为准。**该附注 §五有一道要 Nature 拍板的选择题**：作者声明线能否经宿主接受后放行不可逆动作（甲：能，已按此写入；乙：不能；丙：不需宿主接受）；否决甲即回退到乙。六项在附注各条内。

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B128 | 作者可在 `cut` 上写 `declare: {hi, lo?}` 作作者声明线：按写的数切（不平移、不取严、不被记录替换、不进 `CalibStore`），等级 `Declared`，路由可用；放行不可逆 `do` 须宿主接受声明（CLI `--release-on-declared` / `EntryArgs.accept`，进 `entry_hash`、报告可见），未接受即 `check`/`release` 停下并给修法、不静默降级；报告逐出口列证据量（标注数、线附近读数密度、同键认证线对照）；J-03 不改实质（拦的是自称校准线的东西），新增与 B108 同构的声明。采纳 | `12` §2.2 `cut` 签名、§2.3 `CalibRef` 条、§3 J-03 行；`20` v2 §2.3 `EntryArgs`、§3.4 等级表与 `releases()`、§4.4 第 9 条；`21` 新步 20j-1、20j-2、§四·13；`00-定位与方法论` §八第 5 条注 | 已裁，未实现（20j-1、20j-2）〔B137 订正：账本 `calib_ref` 写 `line: "declared"`，`kind` 留题类〕 | `12` 把读数空间的字面量与校准线当同一件事；评估 ② 与 B108 把「CLI 上没写法能放行」只归因于 taint，漏了等级 |
| B129 | 策略表达三式都在 `cut`：`declare`（读数空间，零标注）、`cost: [fp, fn]`（动作空间，需标注；表层与 `commission_costed` 已造，`12` :461「均已定未造」订正，CLI `--cost` 随 20a-2）、`alpha`（`cut` 上只选同键证书 α ≤ a 最宽一张）；`declare` 在场即定线，与 `cost`/`alpha` 同给 `E-cut-options`；一个字不写沿用记录、无记录即冷、语言不给默认线；裸数字仍 J-03 错，修法句给 `declare` 改写。采纳 | `12` §2.2 签名、§3 B29 段订正；`21` 20a-2 一句、20j-1 | 已裁，未实现（20j-1、20a-2 `--cost`） | `19` B29 行「前端补 `cut` 的 `cost`」已在 `e99455d` 实现而依据文本未回写 |
| B130 | 不用作者标注的四档：题库已认证题式、真值可算、强模型代标加复核、作者声明线；必须标注的只有「要语言替作者担保错误率」一种；「提前标定」作程序或策略的前提不成立、作错误率保证的前提成立且无法绕过；冷出口诊断列四条出路，GUIDE 按此写。采纳 | `12` §3 B29 段注；`21` 20j-1 (4)、9b GUIDE、`题库/规范.md` 一句 | 已裁，未实现（20j-1、9b） | 对外通知第一版把标注写成「先做的一步」 |

## 一·续（B131–B137）进入依据文本（2026-09-25，库层出口合成与待补批 3）

来源 `地基/附注/2026-09-25-库层出口合成与待补批3裁定.md`。六项在附注各条内。

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B131 | 出口的第二个来源是内核合成构造 `compose(exits, rule)`，规则集封闭五种（`any`、`all`、`first(k)`、`sup`、`min`，Kleene 正则），`.jpp` 仍写不出 Act / Ignore；`tally`、`first_k`、`worst_*`、K5 是它上面的 `.jpp` 函数；合成出口 taint 取 ∨、带 `parts`、Unsure 时吸收分量责任；放行 = 全部分量放行之合取、谱系穿过分量、`grade: None ⇒ 不放行`；取代 25-1「一律不作证据」，「按见证分量」不采。采纳 | `12` §2.3 新条、§5 B57 注；`20` v2 §2.4 行、§3.4、§五 S3、改动清单；`21` 步 25 施工注 | 已裁，未实现（25-2、25-9、20j-1 跨轨） | B57、B51-R1 把「只读出口」与「签发出口、并入责任」混为「不需内核能力」；20a-1 让 `grade: None` 缺省放行 |
| B132 | `iterate` 留在内核：它是 J-06 三条终止线的契约值形式，账本键重复判定与循环期证据收集是 `.jpp` 拿不到的能力；`loop` 是其退化形式，不加返回原因的变体，合并与否由步 32 定；`patience` 作 `iterate` 度量选项（内核）。采纳 | `12` §2.7 B132 注、§5 B57 注订正；`21` 步 25 施工注（25-10 撤销、25-10′） | 已裁；25-2 登记、25-10′ 未做 | B57 把「不直接读答案」当成「不需内核能力」 |
| B133 | `sieve` 三处旁写（选择边、`exits` 行 `index`/`pos`、元素形状）收进内核构造 `element(input, exit, ctx)`，`cut` 不改；`parents` 由 B84 值级来源覆盖、`with_parents` 退役；`pair` 两侧 sources 在 25-6 核。采纳 | `12` §2.12 B133 注；`20` v2 §五 S3；`21` 步 25 施工注 | 已裁，未实现（25-2、25-11、25-6） | 规划把「桥写」当唯一出路；约束是「谁能写」不是「在哪写」 |
| B134 | 步 21 Fn¹ 三条边界接受为 B52 实现口径：调用者帧责任不判 Fn¹（唯一性证不了，不收紧，非放行方向）；「复制」= 经任何别名的第二次调用；闭包创建遍历代价属实现。采纳 | `13` §3 B52 注；`20` v2 §3.5 | 已裁（步 21 已合入） | 无 |
| B135 | B92 (b) 按 18c：J-02 只看状态材料，题的来源边不进 J-02、进跳数与谱系；B92 测试 (b) 原文是笔误。确认 | `12` §3 J-02 B92 注 | 已裁（18c 已合入） | B92 测试 (b) 把「由出口生成的题」写成了 J-02 对象 |
| B136 | B127 过渡守卫扩到 `--calib-out`：非 `fixed`/`live` 后端同报 `E-calib-model`；20a-2 后一并删。确认 | `12` §2.2 B127 补；`21` 15g-1 | 已裁，未实现（15g-1 或 15g） | 无 |
| B137 | `calib_ref.kind` 只放题类，线来源另设 `line`（现只 `"declared"`）与 `hi`、`lo`、`site`，18a 留位、20j-1 填；B128 原文 `kind: "declared"` 订正。确认 | `21` 20j-1 (2)、步 18 B124 注；`20` v2 §3.7 (1) | 已裁，未实现（18a、20j-1）〔B142 订正：20j-1 不填四字段，声明线以 `CalibUsed` 条目入账〕 | B124 与 B128 各自占用 `kind` |

## 一·续（B138–B142）进入依据文本（2026-09-25，库层批 4）

来源 `地基/附注/2026-09-25-库层批4裁定.md`。六项在附注各条内。

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B138 | 八个不声明能力的构造：题值构造（`test`/`select`/`measure`/`form`/`fill`）用的是 B57「签发内核值」的题值半，补能力类 `IssueQuestion` 留内核；`unsure_bound` 的能力是刷新点，闸门判据改「`privileges` 与 `refresh` 皆空才无理由在内核」；`key_of` 是读法内置，移出构造表；`pair` 随 25-6 出内核。采纳 | `12` §5 B57 注；`20` v2 §五 S3、§2.3 `constructs` 行；`21` 步 25 施工注（25-2c） | 已裁，未实现（25-2c） | 25-2 把「内核值签发」只映射到出口与读数，漏了题值；`key_of` 按拆臂位置登记而非按性质 |
| B139 | 能力类表冻结为依据文本（十三行：25-2b 十类 + `IssueQuestion`、`RefreshPoint`，`Readings` 拆 `LedgerRead`/`LedgerWrite`）；加类、并类、撤类、改 API 面须 B 裁定；「声明者」不冻结；`IssueUnsure` 过渡、25-11 后撤已预授权。采纳 | `12` §5 B57 注表；`20` v2 §五 S3；`21` §五·1 一行 | 已裁，未实现（25-2c 拆分） | 25-2b 的 `Readings` 读写不分，`outcome`、`sieve` 只读却持写权 |
| B140 | `.jpp` 版 `tally` 对无出口成员报 `E-tally-no-exit`；不静默丢弃、`compose` 不收「种类 + 可选出口」、不记 `Unsure(no_exit)`；25-9 须与 `releases()` 合取同一提交落。采纳 | `12` §2.3 B131 条；`21` 步 25 施工注（25-9） | 已裁，未实现（25-9） | 25-2b 把「照现行计数」当 R 保持项，没看到它在合取下是放行口子（空分量集合取为真） |
| B141 | `first_k` 按 B131 (3) 吸收全部未决分量；`compose` 的 `.jpp` 面无「吸收集合」参数；`tally` 金样 `pending` 元素条目随 25-9 预注册消失；逐元素续判走 `resume.at` 与 `undecided(r)`。采纳 | `12` §2.3 B131 条；`21` 步 25 施工注（25-9） | 已裁，未实现（25-9） | 25-2b 为守 R 把「吸收集合由调用者给」写进内部接口 |
| B142 | 声明线入账取 (d)：`cut` 声明分支追加 `CalibUsed` 条目（键 `declared:<校准键>@<site>`，记录 `{line, hi, lo, site}`），格式不变；`declared:*` 键在站点处与程序当前声明值比，不同报 `W-header` 不拒绝；Judge 条目四字段 20j-1 不填、留位、20a-2 定去留。主会话的 (b)「靠程序哈希」前提不成立（账本头无程序哈希）。改判 | `21` 20j-1 (2)；`20` v2 §3.7 (1)；本表 B137 行指针 | 已裁，未实现（20j-1） | 主会话与首读都以为账本头有程序哈希；B137「20j-1 填」没核 flush 时机 |

## 一·续（B148–B151）进入依据文本（2026-09-25 晚，搭配层）

来源 `地基/比赛/附注-2026-09-25-搭配层设计与36小时施工.md`（§二 原则、§三 原语、§五 落点、§六 条文提案）；断点核对 `地基/评估/2026-09-26-搭配意图落实断点核对.md`。六项在 `12` 各条内。Fable 依 Nature 2026-09-23 授权写入，Nature 可点名否决。

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B148 | 搭配层三条库骨架 `search` / `ground`·`verify` / `judged_graph`·`on_graph`·`interval`：普通函数值、输入输出契约值、可嵌套；未决 `carry` 上传；出口 `compose` 合成；数值产物区间 + `differs` 非空不放行；数字不流；三者闭环 = `search` 带 `ground`。采纳 | `12` §2.3 B148 条、§5 搭配层行；`20` v2 §3.6、§五 S13；`21` E8、P0–P11、§四·14（25-8a、25c、25d、25e）、25b/30b 前移 | 已裁，未实现 | 起草时曾写成演示程序，Nature 更正为底层 |
| B149 | `gen` 真机端口由宿主注册任一生成器（首个 `claude -p`），非阻塞 `submit`/`poll`，同层与 `judge`/`do` 并行；生成器画像 `taint_out` 缺省 `untrusted`；按结构需要时调用。采纳 | `12` §2.4 B149 条；`20` v2 §2.3 L6、§2.5 批调度注、§五 S12；`21` 步 15h（序 P1，库轨） | 已裁，未实现 | 无 |
| B150 | 宿主 `actions/` 动作表：执行器 `exec_py`/`exec_sql`/`check_tests`、精确算法 `graph:*` 六个、检索 `embed_topk`/`bm25_topk`，各带可逆位、`taint_out`、成本；输入只收元素集合不收读数；L-025 与 G-014 由 24e-3 补全。采纳 | `12` §2.5 B150 条；`20` v2 §2.1 L6、§2.3 L6、§五 S14；`21` 步 24e-1、24e-2、24e-3（序 P2、P3、P11） | 已裁，未实现 | 无 |
| B151 | 搭建 / 日常两段式：搭建程序 `gen` 出产物按缓存键复用，日常程序只判、回退分支才 `gen`；报告分列 `gen`/`judge`/`do`；步 19 合入前以材料文件落盘。采纳 | `12` §5 搭建产物行；`21` 步 19 追加项（序 P8，运行时轨） | 已裁，未实现 | 无 |
| B152 | `05` §8 建造顺序订正注：搭配类能力（生成器端口、执行器与精确算法动作、搜索骨架、判图骨架）与内核同层推进、不排末端；第 3 步后紧接搜索与判图骨架，原第 6 步只留分治骨架；原文保留。采纳 | `05-语言本质-v2.md` §8 第 7 条后〔B152〕注；`21` E8 为其工程形式 | 已裁（设计层文本，无实现项） | 无 |

## 一·续（B143–B147）进入依据文本（2026-09-25，批 5）

来源 `地基/附注/2026-09-25-批5裁定.md`。六项在附注各条内。

| 条 | 一句话（是否采纳） | 所改文件与位置 | 状态 | 理解偏差与更正 |
|---|---|---|---|---|
| B143 | 固定观察查线的 `model`：记录只写读数真实来源（jev 认证的记录不得改记 `fixed-0`，B122 诚实判据）；固定端口的模型 id 取夹具顶层 `model` 声明（生成脚本从来源账本头写入）、缺省 `fixed-0`，账本头与查线分量随之，B127 字面不变；带声明的固定运行 `--calib-out` 报 `E-calib-model`；20a-2d 副本方案撤；B116 推翻条件 (2) 只指预注册未列原因的金样变化。裁定 | `12` §2.2 B60 条〔B143〕注；`21` 步 20a-2〔B143〕注 | 已裁，未实现（20a-2d、20a-2e） | 20a-2 规划把固定观察当成可命名的判断器实例；更正为回放端口以被回放读数的实例为 id（`ReplayPorts` 先例） |
| B144 | 代价线线下只出 `unsure(band)`、不出 `ignore`：`commission_costed` 只对放行侧给证书，`ignore` 是被消费的已决否定、须自有证书；下侧代价线登记为方向不立步。确认现行 | `12` §3 J-03 B29 段〔B144〕注 | 已裁，已实现（现行口径，20a-2a P3 测试钉住） | 20a-2a 把「线下即不放行」读成可映射为 `ignore`；三值出口里「不放行」有两个值 |
| B145 | 步 23c 四条解读接受为 B94 实现口径：检视点判据「结果依赖于出口的操作」（六项是例子，函数返回前必解析）；提升分组按运行期 `StateHash`（`21` 「同一状态名」订正）；递归只挡一层属「不保证合批」；公开面四处按 §五豁免登记。采纳 | `12` §2.2 B94 注〔B145〕；`21` 步 23c〔B145〕注 | 已裁，已实现（23c 分支，待三条阻塞修后合入） | `21` 把「同一状态」写成「同一状态名」；六项检视点被当穷举 |
| B146 | B23 `E-fill-type` 机械规则：题式槽 `accepts` × 填法站点声明 `kind`，两侧都有且矛盾才 error，缺任一侧不报，不看实际填值；候选「Test/Select × abstract 即报」不采（会报掉 B23 推荐的「话题相关」写法）；措辞规则与成员资格口径归题库闸门；填法声明生产 `slot_kinds`。裁定 | `12` §2.2 B23 条〔B146〕注；`21` 步 24〔B146〕注 | 已裁，未实现（步 24；20a-2e 取 `slot_kinds`） | 24f 把「直接写出」当成可映射到 op 的结构属性；B23 未说填法类别从哪来 |
| B147 | `12` B51-R2「H4/H5 为假或未测」订正为 B39 不利侧（H4 为真或未测、H5 为假或未测）；24g 两张映射（`Enough` → `count`、`Subset` → `count`/`dedup`，七类留白；`count`/`arithmetic` → H5、`order`/`boundary`/`dedup` → H4）采纳为实现口径。订正 + 采纳 | `12` §2.5 B51-R2 条订正与〔B147〕注；`21` 步 24〔B147〕注；本表 B51 行指针 | 已裁，已实现（24g） | B51-R2 首稿把 H4 方向照 H5 样式写成「为假」，B 栏补齐时未对照 §1.2 字段定义 |

## 二、总账冲突裁定（2026-09-23）

| 冲突 | 一句话（是否采纳） | 所改位置 |
|---|---|---|
| #17 | 缝为八条；00-目标 §3 的六条是 09-22 采样前状态 | `00-目标与动机` §3 注（已加） |
| #18 | 效应同级；「核心效应」改称「唯一产出读数的效应」 | `00-定位与方法论` 术语表（Fable 已改） |
| #19 | 「题式」专指题模板；12 的分类义改称「题类」 | `12` §2.2、§5（已改） |
| #20 | 「一百类写得出」是必要门槛，「验收三条」是充分条件，不冲突 | 无需改动 |
| #22 | 总账 G-034「组合封闭性只是口味」被 B17 取代 | 设计总账条目改桶「被取代」 |
| #23 | 总账 G-037 四级阶梯降为辅助标签，进度只按总账桶计 | 设计总账条目改桶「被取代」 |
| #25 | 成本系数移入画像 `profile.cost.regression`，规则只引用字段 | `12` §2.2 画像条（已改；代码仍写死价格，未实现） |
| #10 | 算子计数按 05 §9 取七（加选、序） | `05` §2 前注（已加） |
| #8 | Readings 的同题跨运行合并改称 `repeat`，材料集合聚合保留 `agg` | `12` §2.2、§5 判断向量行（已改；代码未改名） |
| #9 | 校准键：见 B2 | 同 B2 |
| #51 | 新生成的题不借模式级线，只冷出口或经类键：见 B44 | 同 B44 |
| #58 | 运行期记账（K-048）是本版强制点，局部线性判断形式（K-146）是静态目标形式，不冲突：见 B52 | 同 B52 |
| #59 | 计划期只有可靠下界超预算才拒，其余告警：见 B42 | 同 B42 |
| #61 | 未测字段按逐行方向取值，J-15 已改：见 B39 | 同 B39 |
| #68 | 严格单调与打分耐心停并列为两种变式：见 B43 | `12` §2.7 B43 条 |

## 三、六项说明

各条「为什么、依赖、推翻证据、影响」在 B 栏文件对应条；「替代与放弃理由、怎么改」在 §三（B2、B23、B25–B27 在 §五；B33–B36 六项自含于各自条内，B36 在 §九；B37–B61 六项自含于 `20` 附录 A 各条）；「理解偏差与更正」见上表末栏（来源：B 栏文件 §五 逐条表）。B1–B36 六项已全部落位。B62–B70 六项自含于 `附注/2026-09-24-探针首轮裁定.md` 各条内（§一·续二）。B71 六项自含于 `附注/2026-09-24-施工中裁定.md`（§一·续三）。B72–B77 六项自含于 `附注/2026-09-24-评估①裁定.md` 各条内，A-6 的技术口径六项在该附注 §六末（§一·续四）。B37–B61 六项全部落位：B51 的 R1、R2、C1 曾因缺逐项推翻条件与替代与放弃退回，2026-09-23 晚补为 `20` 附录 A B51-R1、B51-R2、B51-C1 后写入依据文本（总账 L-069、K-240、K-243 改为已定未造）。原第三节三项（停岗、Rust 行、桶制）已由 B25–B27 裁定并进入依据文本。

## 四、仍归 Nature

- 单次真机实验预估超过 5 美元（2026-09-23 晚由 2 美元上调）。
- 人工抽检：R 题式追加 5 条已完成（2026-09-23，5/5 一致，累计 30/30，下界约 0.905），已转正。以后的人工抽检照此归 Nature。〔2026-09-23 晚授权后被 B36 取代：不再请 Nature 抽检，复核由 Fable 做；Nature 自愿做的抽检作为 B36 推翻条件里的「可得人工真值」使用〕
- 点名否决本单任一条。
- 他明确要求讨论的事。

---

内核层条目待设计总账内核层落地后补入。
