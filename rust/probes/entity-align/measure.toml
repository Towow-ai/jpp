# 表达量比计量（验收 1，A-6）。口径与参考系：地基/附注/2026-09-24-评估①裁定.md §六·2–3；
# 出处：地基/研究/07-语言倍率参考系-出处核实.md、地基/研究/08-表达力换算与DSL实测倍率.md §七。
# 行号区间手工标注；改分类即改本文件，scripts/measure_expr.py 只计数与核对。
# 2026-09-24 B78、B80（地基/附注/2026-09-24-仪表读数1裁定.md §一、§二）：基线两口径全计；runtime 只报占比、不从口径里扣；
# 主读数为折行归一行数比（100 列、CJK 双宽），原始行数比、token 比、调用数比并列；caliber1_band 同作折行归一的带。
[reference]
frame = "专用语言对通用语言（J++ 源码按 COCOMO II 生成器计数规则当作写给生成器的输入）"
caliber1_band = [9.0, 20.0]   # T1 的带（B79；Nature 2026-09-24 确认验收 1 按 T1 判）； 只算专用语言文本：GAL 9×、Jones C→SQL 9.8×、TranscriptSQL 与 Sawzall 10×、BOOM-MR 16.6×
t0_band = [2.7, 4.3]         # T0（最小任务书）对 LMQL 带：同规模同结构，基线 34–78 行（B79）
caliber2_band = [3.5, 4.6]    # 计入生成器或胶水：GAL 3.5×、BOOM-MR 3.9×、QSM C→SQL 4.6×
time_band = [1.45, 6.0]       # Sprat 1.45–2.3× 到 RISLA 4–6×；只报位置，不设通过线
control = "弱受控：单份 J++ 对 1 份基线；J++ 与基线作者不同人"

[project]
name = "entity-align"
original = "TypeSafe cookbook: Knowledge graph entity alignment（候选对匹配）"
shape = "打分题一道加是非题三道同状态；逐对；1 层"
projection = "entity-align"   # probes/_baseline/accept.py 里的投影函数

[jpp]
run_dir = "."
source = "align.jpp"
fixture = "fixture.json"
input = "baseline/materials.json"          # 材料经 jpp run --input 交给程序（步 14b-0）
data = "20"               # 数据行只剩标签集 outcome_of（程序自己的动作名，不是材料）
coordination = "5,25,36"      # 协调行：budget、未决去向、pending 转交等服务于八条缝的行
minutes = "无数"
minutes_note = "探针首轮未计时"

[[jpp.glue]]                       # 夹具脚本：两侧共用的测试设施（基线经不计行的 SDK 读同一份夹具），只报不计（B78）
what = "夹具脚本"
shared = true
file = "make_fixture.py"
data = "4-8,14-16"          # 读 materials.json 的两份目录（步 14b-0）与档位文字副本、合成读数

[[jpp.glue]]                       # 口径二计入：生产运行必需的胶水（CLI 或 Session 调用、动作登记、calib-import）
what = "CLI 调用（jpp run … --fixtures …）"
lines = 1

[baseline]
dir = "baseline"                   # 任务书、materials.json、expected.json 与基线实现所在目录

[[baseline.impl]]
file = "entity_align_a.py"
author = "Sonnet 5 子代理（未参与 J++ 设计，只见任务书、materials.json 与判断接口；2026-09-24 派发）"
data = "13-14,39-40,42-44"               # 读 materials.json 与拆包的行
runtime = "17-22,25-35,64-67"         # 运行时职责：阈值换算、三态分支、未决收集
coordination = "17-22,25-35,65-67"
minutes = 0.6           # 代理墙钟 15:40:41 → 15:41:17（拿到任务书到首次通过验收）；与 J++ 侧作者不同，不计时间比
passed = "2026-09-24 python3 scripts/measure_expr.py --check entity-align：通过（一次通过）"

[[baseline.impl]]                  # T0 第二份基线（步 31-1，另一个 Sonnet 代理，同规程）
tier = "t0"
file = "entity_align_b.py"
author = "Sonnet 5 子代理（未参与 J++ 设计，只见任务书、materials.json 与判断接口；2026-09-24 步 31-1 派发）"
data = "19-21,54-57"
runtime = "33-50,79-80"
coordination = "33-50,79-80"
minutes = 0.77
passed = "2026-09-24 --check entity-align：通过（代理墙钟 20:05:36→20:06:22）"

[[baseline.impl]]                  # T1 基线（步 31-1，Sonnet 代理，只见任务书 T0+T1 节、t1/materials.json、labels.jsonl 与判断接口）
tier = "t1-strict"                # 步 31-1b（B96）：旧 (d) 逐位口径，只报不判；验收用冻结的 t1/labels-strict.jsonl
file = "t1/entity_align_t1_a.py"
author = "Sonnet 5 子代理（未参与 J++ 设计；2026-09-24 步 31-1 派发）"
data = "278-279"
runtime = ""
coordination = ""
minutes = 4.53

# —— T1（完整职责任务书，B79；步 31-1）——
passed = "2026-09-24 --check entity-align --tier t1：一次全过（20:09:04→20:13:36）"

[[baseline.impl]]                  # T1 基线（步 31-1b，B96 性质口径：(d) 只写性质、考官留出集验收）
tier = "t1"
file = "t1/entity_align_t1_b.py"
author = "Sonnet 5 子代理（未参与 J++ 设计；2026-09-24 步 31-1b 派发，只见 T0+T1 任务书、t1/materials.json、labels.jsonl 与判断接口）"
data = ""
runtime = ""
coordination = ""
minutes = 5.88
passed = "2026-09-25 --check entity-align --tier t1：一次全过（重派 00:40:30→00:46:23；首派 23:11 停住未落盘）；(d) 全集最大覆盖 CP。作者自报误看了 expected-t1.json 前 300 字节（(d) 已写完之后）"

[[baseline.impl]]                  # T1 基线（步 31-1b，B96 性质口径：(d) 只写性质、考官留出集验收）
tier = "t1"
file = "t1/entity_align_t1_c.py"
author = "Sonnet 5 子代理（未参与 J++ 设计；2026-09-24 步 31-1b 派发，只见 T0+T1 任务书、t1/materials.json、labels.jsonl 与判断接口）"
data = ""
runtime = ""
coordination = ""
minutes = 10.6
passed = "2026-09-25 --check entity-align --tier t1：一次全过（01:20:38→01:31:14）；(d) 按同 p 分组的最大前缀 CP"

[t1]
fixture = "fixture-t1.json"        # 与 T0 夹具同一份读数，去掉校准记录字面量；线只从 baseline/t1/labels.jsonl 经 calib-import 来
calls_max = 9                    # (c) 调用上限：每层每份材料至多一次（任务书 T1 节）
rounds_max = 1                   # (c) 轮数上限（J++ 侧取账本里不同 layer 的个数）

[[t1.glue]]                        # 口径二计入：T1 的线从标注导入，生产运行必需
what = "calib-import 调用（jpp calib-import labels.jsonl --calib-out …）"
lines = 1

[[jpp.impl]]                       # J++ 设计者 T1 版：探针 3 加 unobserved（施工代理改写，步 31-1）
tier = "t1"
source = "t1/align_t1.jpp"
run_dir = "t1"
projection = "entity-align"
author = "设计者版 T0 + 施工代理按 T1 任务书改写"
input = "baseline/t1/materials.json"          # 材料经 jpp run --input 交给程序（步 14b-0）
data = "20"
coordination = "5,25,36,37,38,48,49"

[[jpp.impl]]                       # J++ 非设计者 T0 版（步 31-1，Sonnet 代理，只见任务书、GUIDE.md 与 lib/outcome.jpp）
tier = "t0"
source = "jpp-nd/entity_align_t0.jpp"
run_dir = "jpp-nd"
author = "Sonnet 5 子代理（未参与 J++ 设计；2026-09-24 步 31-1 派发）"
data = "50-51"
coordination = "1,9,18"
minutes = 33.92
passed = "2026-09-24 --check entity-align：通过（20:34:02→21:07:57）"

[[jpp.impl]]                       # J++ 非设计者 T1 版（步 31-1，Sonnet 代理，只见任务书、GUIDE.md 与 lib/outcome.jpp）
tier = "t1"
source = "jpp-nd/entity_align_t1.jpp"
run_dir = "jpp-nd"
author = "Sonnet 5 子代理（未参与 J++ 设计；2026-09-24 步 31-1 派发）"
data = "39-40"
coordination = "1,8,12,16,20"
minutes = 22.45
passed = "2026-09-24 --tier t1：(b) ① 不过（打分题不能进 sieve，judge 上预算用完整程序 Pending）；其余通过（21:19:57→21:42:24）"
