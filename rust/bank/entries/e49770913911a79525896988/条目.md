# 条目 which_named · `e49770913911a79525896988`

**状态：已认证·未复用**（B118；首批 F3，2026-09-25 入库）。等级：正式（Form），线在**两序读数**上认证。按 `地基/题库/规范.md` v2 §一写。

## 1.1 题式

- **题型**：`select`（K 选一，单侧线，B63）。
- **模板**：`这段话里直接写出了下列哪一个{kind}的名字？`，题式记录 `{calib: "cls-which-mentioned", over_kind: "labels", permute: true}`，定义在 `lib/bank/which_named.jpp`。
- **置换声明**：`permute: true`（B64，步 15f）。它不进 `form_hash`，但决定测量：真机每道题发正序、逆序两次，读数取两次概率均值，记 `perms = 2` 与 `mode_share`。本条的线就在这种读数上认证；去掉声明，真机出口是 `unsure(untested:permutation)`。
- **题的五件**：主体 = 一段话；谓词 = 直接写出了某个候选的名字；划分 = K 个候选标签（程序写定）；请求 = 分布（`choice`）；前提 = 无声明。
- **题类**：归类（声明 `over_kind: labels`）；记录 `kind: class`（经 `--report` 取，B120 (a)）。
- **calib 字段**：`cls-which-mentioned`。
- **标注口径**：真值是「材料里逐字出现的那个候选」；候选外的同类名字（挪威、泰国、加拿大、武汉）不算；认证集每条恰好写出一个候选，半数另带一个候选外的同类名字作干扰。材料里写出两个及以上候选的情形不在认证范围内。

## 1.2 槽与可接受填法类型

`kind`：类别（认证用「城市」「国家」）。成员资格口径：常识口径的专名类别。候选标签须是该类别的专名，由调用程序写定。

## 1.3 校准记录与等级

- 记录：`calib/_form_e49770913911a79525896988.json`；标注集：`labels.jsonl`（64 行，`source: computed`，`p`/`pick` 为两序均值的 p_max 与 argmax）。
- 认证：B86 固定序、B63 单侧；α 0.1，conf_delta 0.1，δ 0.0781。hi 0.9169（即候选阈值 0.995 减 δ）；已决 63/64，0 错。
- 两序测量：64/64 两序众数一致（`mode_share = 1`，没有位置偏置），均值 argmax 与真值 64/64 一致，最低 p_max 0.99。
- **已决率**：按认证口径 63/64。认证集上的运行时重放是 58/64，更严：5 条读数正好 0.995，而运行时判 `p_max ≥ hi + δ` 时浮点下 0.9169 + 0.0781 = 0.9950000000000001，这 5 条落进带内。**15d-2 统一边界前，运行时重放口径更严**（主会话 2026-09-25 定，边界并入效应轨 15d-2）。方向是少放行，不是多放行。
- 装载：`load` 重跑认证逐位复现，无写回。

## 1.4 诊断记录（B13）

`jpp check`：0 告警。原模板问「提到」，触发 `W-diag-mention-scope`，按 B13 改为「直接写出了……的名字」。

## 1.5 使用统计

由步 27 的 `jpp bank-stats` 生成；入库时为 0。

## 1.6 来源

材料：`地基/评估/27a-准备/F3-提到哪一个/`（单序冷跑 64 次，0.001061 美元，其记录已作废，见该目录 `作废-单序测量.md`）；两序重跑与认证：`地基/评估/27a-置换重跑/F3-提到哪一个/`（128 次请求，0.002122 美元）；预注册 `过程记录/工程-步27a.md` §一·1（提交 `39e969b2`）。

## 1.7 版本

身份 = `e49770913911a79525896988`。单序记录与本记录同键（`permute` 不进哈希），单序那条不入库。

## 六项

1. **为什么要它**：首批「归类 · K 标签」一格（B90）；也是 H5「两序均值读数在固定序单侧认证下仍得正式线」的检验对象（成立）。
2. **考虑过的替代**：单序认证——放弃，15f 后真机 `select` 的测量是两序，单序线与在用读数不是同一测量；两序不一致的条目从标注集剔除——放弃，看过数据后的选择。
3. **依赖与被依赖**：依赖 B63、B64（15f）、B86；运行时边界比较依赖 15d-2。无程序依赖它。
4. **什么会推翻它**：在用中 `mode_share < 1`（出 `tie`）的比例明显上升，说明位置偏置在认证范围外材料上出现；或材料写出两个候选时判断器稳定选错——需要新的题式或扩范围（B91）。
5. **怎么改**：改模板或去掉 `permute` 声明的用法都要重认；改模板即新条目。重跑 `27a-置换重跑/F3` 的 `read.jpp`（冻结后重放），`build_labels_perm.py`、`calib-import --report`。
6. **理解偏差与更正**：27a 准备在单序读数上认证，出口全是 `untested:permutation`；15f 之后改为两序重认。认证与重放已决数不一致（63 对 58）起初看像预测落空，实为浮点边界，已报主会话。
