# 条目 amount_left · `e4d4be39383a1eb88f75af2c`

**状态：已认证·未复用**（B118；首批 F5，2026-09-25 入库）。等级：正式（Form）。按 `地基/题库/规范.md` v2 §一写。

## 1.1 题式

- **题型**：`measure`（有序三档，单侧线，B63）。
- **模板**：`这段话里说的{thing}还剩多少？`，档位 `["已经没有了", "只剩一点", "还有很多"]`（档位进题式哈希），定义在 `lib/bank/amount_left.jpp`。
- **题的五件**：主体 = 一段话；谓词 = 物品 thing 的剩余量；划分 = 三档有序；请求 = 分布（`score`）；前提 = 无声明。
- **题类**：程度（`question_kind` 推出）；记录 `kind: degree`。
- **calib 字段**：`deg-amount-left`。
- **标注口径**：真值按材料里描述 thing 余量的短语归档（认证集的三张短语表互不重叠：如「全部断货」「早就用光」→ 没有；「所剩无几」「只剩个底」→ 一点；「库存很充足」「够用好几个月」→ 很多）。认证集半数另写一种物品及另一档余量，只按 thing 那一处判。材料没有说 thing 的余量时不在认证范围内。
- **置换**：不适用（`measure` 不做置换，15f 只对 `select`）；27a 准备实测 `measure` 不受 J-15 置换项阻塞。

## 1.2 槽与可接受填法类型

`thing`：具体物（物品名）。不接受类别（「办公用品」）。

## 1.3 校准记录与等级

- 记录：`calib/_form_e4d4be39383a1eb88f75af2c.json`；标注集：`labels.jsonl`（65 行；材料 66 条，其中 d48、d54 文本与填法相同、同一账本键，按一个样本计）。
- 认证：B86 固定序、B63 单侧；α 0.1，conf_delta 0.1，δ 0.1141（证书 `rule` 内）。hi 0.8159；已决 65/65，0 错。
- 装载：`load` 重跑认证逐位复现，无写回。
- 认证集上的重放：已决 65/66，全对。

## 1.4 诊断记录（B13）

`jpp check`：0 告警。

## 1.5 使用统计

由步 27 的 `jpp bank-stats` 生成；入库时为 0。

## 1.6 来源

`地基/评估/27a-准备/F5-还剩多少/`（65 次真机调用，0.000927 美元）；预注册同 F1。

## 1.7 版本

身份 = `e4d4be39383a1eb88f75af2c`。首次入库。

## 六项

1. **为什么要它**：首批「程度 · 单对象」一格（B90）；也检验了 H3 的范围：`measure` 在真机线下正常出 `at` 并可放行，15f 的阻塞只对 `select`。
2. **考虑过的替代**：五档刻度——放弃，认证集的构造短语在三档上才能互不重叠；连续数值（「还剩几成」）——判断器不做算术（能力画像），不取。
3. **依赖与被依赖**：依赖 B63、B86、B104（δ）。无程序依赖它。
4. **什么会推翻它**：在用中材料用数字表述余量（「还剩 3 卷」）时带内率明显上升——认证集没有数字表述，需按 B91 扩范围或拆题式。
5. **怎么改**：改模板或档位即新条目；材料在 `27a-准备/F5-还剩多少/make_materials.py`（固定种子）。
6. **理解偏差与更正**：首次导入把同键的两条材料计为两个样本，已按账本键去重后重导入（n 65），线不变。
