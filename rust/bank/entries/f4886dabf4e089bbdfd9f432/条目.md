# 条目 same_name · `f4886dabf4e089bbdfd9f432`

**状态：已认证·未复用**（B118；首批 F4，2026-09-25 入库）。等级：正式（Form），线在**两序读数**上认证。按 `地基/题库/规范.md` v2 §一写。

## 1.1 题式

- **题型**：`select`（K 选一，单侧线，B63）。
- **模板**：`下列哪一段话里也直接写出了这段话里那个{kind}的名字？`，题式记录 `{calib: "cmp-same-referent", over_kind: "candidates", permute: true}`，定义在 `lib/bank/same_name.jpp`。
- **置换声明**：`permute: true`（B64），含义同 which_named：线在两序均值读数上认证，去掉声明真机出 `untested:permutation`。
- **题的五件**：主体 = 一段话；谓词 = 候选段写出了与主体中同一个名字；划分 = K 段候选材料（程序计算出的材料）；请求 = 分布；前提 = 无声明。
- **题类**：比较（声明 `over_kind: candidates`）；记录 `kind: cmp`（经 `--report` 取，B120 (a)；27a 准备时的单序记录按标注行只能算到 `class`，补丁请求 1）。
- **calib 字段**：`cmp-same-referent`。
- **标注口径**：真值是「写出了与主体同一名字（逐字相同）」的那段候选；其余候选写的是同类别的别的名字。认证集每题恰好一段对上，真值位置 0/1/2 各 21 条。同名不同指（两个叫王强的人）不在认证范围内。

## 1.2 槽与可接受填法类型

`kind`：类别（认证用「城市」「人」）。候选由调用程序计算得出，K = 3。

## 1.3 校准记录与等级

- 记录：`calib/_form_f4886dabf4e089bbdfd9f432.json`；标注集：`labels.jsonl`（63 行，`source: computed`，`p`/`pick` 为两序均值）。
- 认证：B86 固定序、B63 单侧；α 0.1，conf_delta 0.1，δ 0.0781。hi 0.8869（候选阈值 0.965 减 δ）；已决 63/63，0 错。
- 两序测量：63/63 两序众数一致（无位置偏置），均值 argmax 与真值 63/63 一致，最低 p_max 0.965。
- **已决率**：按认证口径 63/63。认证集上的运行时重放是 62/63：读数 0.965 那一条因浮点边界（0.8869 + 0.0781 = 0.9650000000000001）落进带内。**15d-2 统一边界前，运行时重放口径更严。**
- 装载：`load` 重跑认证逐位复现，无写回。

## 1.4 诊断记录（B13）

`jpp check`：0 告警。原模板问「提到」，按 B13 改为「直接写出了……的名字」。

## 1.5 使用统计

由步 27 的 `jpp bank-stats` 生成；入库时为 0。

## 1.6 来源

材料：`地基/评估/27a-准备/F4-同一个/`（单序冷跑 63 次，0.001184 美元，其记录已作废）；两序重跑与认证：`地基/评估/27a-置换重跑/F4-同一个/`（126 次请求，0.002368 美元）；预注册 `过程记录/工程-步27a.md` §一·1（提交 `39e969b2`）。

## 1.7 版本

身份 = `f4886dabf4e089bbdfd9f432`。首次入库。

## 六项

1. **为什么要它**：首批「比较 · K 候选材料」一格（B90）；K 候选是计算出的材料，比 which_named 更容易出位置偏置，所以它是 H5 / H6 的主要检验对象（预注册 F4 `tie` 超 1/3 即推翻；实测 0）。
2. **考虑过的替代**：一对 `test`「两条是否同一{X}」（entity-align 三道的写法）——槽形不同，B118 的第二批按谓词级复用证据另选，不在首批替换。
3. **依赖与被依赖**：同 which_named。无程序依赖它。
4. **什么会推翻它**：在用中 `tie` 比例上升（候选材料更长、更相近时位置偏置出现），或同名不同指的材料上稳定选错。
5. **怎么改**：同 which_named；材料在 `27a-准备/F4-同一个/make_materials.py`。
6. **理解偏差与更正**：单序记录的 `kind` 是 `class`，精化类应为比较；两序重认时带 `--report` 取到 `cmp`，与 B120 (a) 的预期一致。
