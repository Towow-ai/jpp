// 元素：检索（do("bm25_topk", …)）。纯 Rust 实现，不需要联网或本地模型，任何机器上都能跑。
// 检索只负责召回，不负责精度：粗筛缩小候选范围后交给判断逐条精判（见〈搭配用法手册〉§一「先便宜后贵」）。
// 另一个检索动作 embed_topk 用本地向量模型，见 elem-retrieval-embed.jpp（只 check，不 run）。
budget {calls: 4, cost: 0, depth: 8};
content(do("bm25_topk", ["苹果", ["苹果 香蕉 橙子", "今天天气很好", "苹果派配咖啡"], 2], 0))
